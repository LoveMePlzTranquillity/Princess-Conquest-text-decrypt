CommonEvent 494
Name = "★👤★ *RND Dials*"

  Script(["@lewdi = $game_variables[2879]"])
  Script(["@lewdmod = 0"])
  Script(["@nat = 0"])
  Comment(["First Setting"])
  Script(["@v = 2792"])
  ScriptMore(["while @v <= 2820"])
  ScriptMore(["if $game_variables[@v] == 0"])
  ScriptMore(["$game_variables[@v] = Array.new(50, 0)"])
  ScriptMore(["end"])
  ScriptMore(["@v += 1"])
  ScriptMore(["end"])
  Comment(["-----------------"])
  Script(["@n = 0"])
  Script(["@v = 2792"])
  Comment(["loop 2 I"])
  Loop([])
    ConditionalBranch([12, "@v == 2809 || @v == 2815"])
      ControlVariables([2879, 2879, 4, 0, 20])
      Script(["@lewdmod = $game_variables[2879]"])
      ControlVariables([2879, 2879, 3, 0, 20])
      Script(["@d = rand(10 + @lewdmod)"])
      Comment(["lewdmod END"])
      ConditionalBranch([12, "@d < 2"])
        Script(["@d = 0"])
        Empty([])

      Else([])
        ConditionalBranch([12, "@d < 4"])
          Script(["@d = 1"])
          Empty([])

        Else([])
          ConditionalBranch([12, "@d < 6"])
            Script(["@d = 2"])
            Empty([])

          Else([])
            ConditionalBranch([12, "@d < 8"])
              Script(["@d = 3"])
              Empty([])

            Else([])
              Script(["@d = 4"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    Else([])
      ConditionalBranch([12, "@v == 2804 || @v == 2811 || @v == 2818 || @v == 2819"])
        Comment(["lewdmod"])
        ControlVariables([2879, 2879, 4, 0, 20])
        Script(["@lewdmod = $game_variables[2879]"])
        ControlVariables([2879, 2879, 3, 0, 20])
        Script(["@d = rand(8 + @lewdmod)"])
        Comment(["lewdmod END"])
        ConditionalBranch([12, "@d < 2"])
          Script(["@d = 0"])
          Empty([])

        Else([])
          ConditionalBranch([12, "@d < 4"])
            Script(["@d = 1"])
            Empty([])

          Else([])
            ConditionalBranch([12, "@d < 6"])
              Script(["@d = 2"])
              Empty([])

            Else([])
              Script(["@d = 3"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      Else([])
        ConditionalBranch([12, "@v == 2814"])
          Script(["@d = rand(4)"])
          ConditionalBranch([12, "@d < 1"])
            Script(["@d = 3"])
            Empty([])

          Else([])
            Comment(["lewdmod"])
            ControlVariables([2879, 2879, 4, 0, 10])
            Script(["@lewdmod = $game_variables[2879]"])
            ControlVariables([2879, 2879, 3, 0, 10])
            Script(["@d = rand(8 + @lewdmod)"])
            Comment(["lewdmod END"])
            ConditionalBranch([12, "@d < 3"])
              Script(["@d = 0"])
              Empty([])

            Else([])
              ConditionalBranch([12, "@d < 6"])
                Script(["@d = 1"])
                Empty([])

              Else([])
                Script(["@d = 2"])
                Empty([])

              BranchEnd([])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        Else([])
          ConditionalBranch([12, "@v == 2800 || @v == 2810 || @v == 2817"])
            Comment(["lewdmod"])
            ControlVariables([2879, 2879, 4, 0, 10])
            Script(["@lewdmod = $game_variables[2879]"])
            ControlVariables([2879, 2879, 3, 0, 10])
            Script(["@d = rand(8 + @lewdmod)"])
            Comment(["lewdmod END"])
            ConditionalBranch([12, "@d < 1"])
              Script(["@d = 0"])
              Empty([])

            Else([])
              ConditionalBranch([12, "@d < 2"])
                Script(["@d = 1"])
                Empty([])

              Else([])
                Script(["@d = 2"])
                Empty([])

              BranchEnd([])
              Empty([])

            BranchEnd([])
            Empty([])

          Else([])
            ConditionalBranch([12, "@v == 2792 || @v == 2793 || @v == 2798 || @v == 2806 || @v == 2812"])
              Comment(["lewdmod"])
              ControlVariables([2879, 2879, 4, 0, 10])
              Script(["@lewdmod = $game_variables[2879]"])
              ControlVariables([2879, 2879, 3, 0, 10])
              Script(["@d = rand(8 + @lewdmod)"])
              Comment(["lewdmod END"])
              ConditionalBranch([12, "@d < 2"])
                Script(["@d = 0"])
                Empty([])

              Else([])
                ConditionalBranch([12, "@d < 4"])
                  Script(["@d = 1"])
                  Empty([])

                Else([])
                  ConditionalBranch([12, "@d < 6"])
                    Script(["@d = 2"])
                    Empty([])

                  Else([])
                    Script(["@d = 3"])
                    Empty([])

                  BranchEnd([])
                  Empty([])

                BranchEnd([])
                Empty([])

              BranchEnd([])
              Empty([])

            Else([])
              ConditionalBranch([12, "@v == 2808 || @v == 2820"])
                Comment(["Bank Dials"])
                Script(["@d = rand(8)"])
                ConditionalBranch([12, "@d < 2"])
                  Script(["@d = 0"])
                  Empty([])

                Else([])
                  ConditionalBranch([12, "@d < 4"])
                    Script(["@d = 1"])
                    Empty([])

                  Else([])
                    ConditionalBranch([12, "@d < 6"])
                      Script(["@d = 2"])
                      Empty([])

                    Else([])
                      Script(["@d = 3"])
                      Empty([])

                    BranchEnd([])
                    Empty([])

                  BranchEnd([])
                  Empty([])

                BranchEnd([])
                Empty([])

              Else([])
                Script(["@d = rand(6)"])
                Comment(["lewdmod"])
                ConditionalBranch([12, "@v == 2796 || @v == 2805 || @v == 2813"])
                  ControlVariables([2879, 2879, 4, 0, 5])
                  Script(["@lewdmod = $game_variables[2879]"])
                  ControlVariables([2879, 2879, 3, 0, 5])
                  Script(["@d = rand(6 + @lewdmod)"])
                  Empty([])

                Else([])
                  ControlVariables([2879, 2879, 4, 0, 10])
                  Script(["@lewdmod = $game_variables[2879]"])
                  ControlVariables([2879, 2879, 3, 0, 10])
                  Script(["@d = rand(6 + @lewdmod)"])
                  Empty([])

                BranchEnd([])
                Comment(["lewdmod END"])
                ConditionalBranch([12, "@d < 2"])
                  Script(["@d = 0"])
                  Empty([])

                Else([])
                  ConditionalBranch([12, "@d < 4"])
                    Script(["@d = 1"])
                    Empty([])

                  Else([])
                    Script(["@d = 2"])
                    Empty([])

                  BranchEnd([])
                  Empty([])

                BranchEnd([])
                Empty([])

              BranchEnd([])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    ConditionalBranch([0, 804, 0])
      Script(["@r = rand(3)"])
      ScriptMore(["if @r < 1"])
      ScriptMore(["@d = \"UndK\""])
      ScriptMore(["end"])
      Empty([])

    BranchEnd([])
    Script(["$game_variables[@v][@n] = @d"])
    ConditionalBranch([12, "@v == 2819 && objective_active?(110,0)"])
      Comment(["plant hints"])
      Script(["@r = rand(3)"])
      ScriptMore(["if @r < 1"])
      ScriptMore(["$game_variables[@v][@n] = 4"])
      ScriptMore(["end"])
      Empty([])

    BranchEnd([])
    Script(["@n += 1"])
    ConditionalBranch([12, "@n > 30"])
      Script(["@n = 0"])
      Script(["@v += 1"])
      ConditionalBranch([12, "@v > 2820"])
        BreakLoop([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  RepeatAbove([])
  Comment(["RND babby dials"])
  Script(["@v = 2250"])
  Loop([])
    Script(["@ski = $game_variables[@v][60] rescue nil"])
    ScriptMore(["@r = rand(6)"])
    ScriptMore([" if @ski == \"Tsundere\""])
    ScriptMore(["   @feelStr = [20,21,22,2,2,2]"])
    ScriptMore([" elsif @ski == \"Yandere\""])
    ScriptMore(["   @feelStr = [30,31,32,3,3,3]"])
    ScriptMore([" elsif @ski == \"Erodere\""])
    ScriptMore(["   @feelStr = [33,34,35,3,3,3]"])
    ScriptMore([" elsif @ski == \"Kuudere\""])
    ScriptMore(["   @feelStr = [10,11,12,1,1,1]"])
    ScriptMore([" elsif @ski == \"Dandere\""])
    ScriptMore(["   @feelStr = [13,14,15,1,1,1]"])
    ScriptMore([" elsif @ski == \"Nemuidere\""])
    ScriptMore(["   @feelStr = [40,41,42,0,0,0]"])
    ScriptMore([" else"])
    ScriptMore(["   @feel = rand(2)"])
    ScriptMore(["   if @feel >= 1"])
    ScriptMore(["    $game_variables[@v][15] = $game_variables[@v][14]"])
    ScriptMore(["    @feelStr = 1"])
    ScriptMore(["   else"])
    ScriptMore(["    @feelStr = [0,0,1,1,2,2,3,3]"])
    ScriptMore(["    @r = rand(8)"])
    ScriptMore(["   end"])
    ScriptMore([" end "])
    ConditionalBranch([12, "@feelStr != 1"])
      Script(["$game_variables[@v][15] = @feelStr[@r]"])
      Empty([])

    BranchEnd([])
    Script(["@v += 1"])
    ConditionalBranch([12, "@v >= 2751"])
      BreakLoop([])
      Empty([])

    BranchEnd([])
    Empty([])

  RepeatAbove([])
  ControlVariables([2879, 2879, 0, 4, "@lewdi"])
  Empty([])
