CommonEvent 887
Name = "💀💀💀 Freeze 💀💀💀"

  ConditionalBranch([12, "$game_message.visible == true"])
    JumpToLabel(["freezeRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 1770, 0])
    Comment(["All Res"])
    JumpToLabel(["freezeRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 4156, 0])
    Comment(["All Res"])
    JumpToLabel(["freezeRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 4182, 0])
    Comment(["FLOW"])
    JumpToLabel(["freezeRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 3998, 0])
    Comment(["Susp Terr"])
    JumpToLabel(["freezeRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 3778, 0])
    Comment(["Ske ending"])
    JumpToLabel(["freezeRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.state?(70) == true "])
    Comment(["Warm Aura Skill"])
    JumpToLabel(["freezeRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.state?(36) == true "])
    Comment(["HOT drinks"])
    JumpToLabel(["freezeRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.state?(57) == true "])
    Comment(["CHILLBLESS"])
    JumpToLabel(["freezeRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.state?(247) == true "])
    Comment(["ClockRES"])
    JumpToLabel(["freezeRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.equips.include?$data_armors[688]"])
    Comment(["BEAST BOOTS"])
    JumpToLabel(["freezeRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.equips.include?$data_armors[176]"])
    Comment(["Chunga Root"])
    JumpToLabel(["freezeRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.equips.include?$data_armors[264]"])
    Comment(["Rabbit Coat"])
    JumpToLabel(["freezeRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.equips.include?$data_armors[376]"])
    Comment(["Molten Core"])
    JumpToLabel(["freezeRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.equips.include?$data_armors[265]"])
    Comment(["Cat Hat"])
    JumpToLabel(["freezeRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.equips.include?$data_armors[366]"])
    Comment(["Captain Coat"])
    JumpToLabel(["freezeRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.id == 6"])
    Comment(["Golem"])
    ConditionalBranch([12, "$game_party.leader.class.id == 28"])
      Comment(["Succubi"])
      Empty([])

    Else([])
      JumpToLabel(["freezeRES"])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.id == 9"])
    Comment(["Skele"])
    JumpToLabel(["freezeRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.id >= 35 && $game_party.leader.id <= 39"])
    ControlVariables([2868, 2868, 0, 4, "$game_party.leader.id"])
    ControlVariables([2863, 2863, 0, 4, "$game_party.leader.id"])
    CallCommonEvent([479])
    Script(["@prog = $game_variables[2868]"])
    Comment(["Prog"])
    Script(["@ski = $game_variables[@prog][60] rescue nil"])
    ConditionalBranch([12, "@ski"])
      Comment(["-------------------------------"])
      ConditionalBranch([12, "@ski == 0"])
        JumpToLabel(["afterSkills"])
        Empty([])

      BranchEnd([])
      Comment(["-------------------------------"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "@ski != 0"])
      ConditionalBranch([12, "@ski == \"ThiccFat\""])
        JumpToLabel(["freezeRES"])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Label(["afterSkills"])
    Comment(["-------------------"])
    ConditionalBranch([12, "$game_variables[@prog][55] == 2"])
      Comment(["chunga mod"])
      JumpToLabel(["freezeRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][55] == 5"])
      Comment(["shiftCore-FreezeMod"])
      JumpToLabel(["freezeRES"])
      Empty([])

    BranchEnd([])
    Comment(["NEWRES"])
    Script(["@resStr = $game_variables[@prog][59]"])
    ConditionalBranch([12, "@resStr != 0"])
      ConditionalBranch([12, "@resStr.include? \"Col\""])
        JumpToLabel(["freezeRES"])
        Empty([])

      BranchEnd([])
      JumpToLabel(["checkRes"])
      Empty([])

    BranchEnd([])
    Comment(["^^^ NEWRES ^^^"])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"goat\""])
      JumpToLabel(["freezeRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"candy\""])
      JumpToLabel(["freezeRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"idol\""])
      JumpToLabel(["freezeRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"dummy\""])
      JumpToLabel(["freezeRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"crystal\""])
      JumpToLabel(["freezeRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"dullanyan\""])
      JumpToLabel(["freezeRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"futadog\""])
      JumpToLabel(["freezeRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"yukionna\""])
      JumpToLabel(["freezeRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"bot\""])
      JumpToLabel(["freezeRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == 4 || $game_variables[@prog][0] == 6  || $game_variables[@prog][0] == 24"])
      Comment(["golem / skeleton / crystal"])
      ConditionalBranch([12, "$game_variables[@prog][20] != \"feather\""])
        JumpToLabel(["freezeRES"])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == 16 && $game_variables[@prog][4] == 2"])
      JumpToLabel(["freezeRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == 16 && $game_variables[@prog][4] == 5"])
      JumpToLabel(["freezeRES"])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  Label(["checkRes"])
  ConditionalBranch([0, 4337, 0])
    JumpToLabel(["inAfflict"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 3691, 0])
    ConditionalBranch([2, "A", 1])
      ControlSelfSwitch(["A", 0])
      ShowTextAttributes(["", 0, 0, 2])
      ShowText(["This area is \\c[18]Freezing\\c[0]!\\.\\. You will take damage over time if your Party Leader doesn't resist it!"])
      ShowTextAttributes(["", 0, 0, 2])
      ShowText(["Equip the leader with \\i[681] \\c[18]Cold Resisting\\[0] equipments or have someone naturally resisting it as the Leader!"])
      Empty([])

    BranchEnd([])
    Label(["inAfflict"])
    PlaySE(["RPG::SE(@name=\"Ice6\", @pitch=100, @volume=80)"])
    FlashScreen(["Color(@red=0.0, @green=153.0, @blue=221.0, @alpha=136.0)", 10, false])
    ConditionalBranch([12, "$game_map.map_id == 366"])
      Script(["@dmg = 30"])
      Empty([])

    Else([])
      ConditionalBranch([12, "$game_party.leader.state?(118) == true "])
        Comment(["Frostbite"])
        Script(["@dmg = 30"])
        Empty([])

      Else([])
        Script(["@dmg = 15"])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Script(["while $game_party.leader.hp - @dmg < 1"])
    ScriptMore([" @dmg -= 1"])
    ScriptMore(["end"])
    ScriptMore(["$game_party.leader.hp -= @dmg"])
    ScriptMore(["message = @dmg.to_s"])
    ScriptMore(["$game_map.show_text($game_player,message)"])
    ScriptMore(["$game_map.sas_hud.refresh_bars"])
    ConditionalBranch([12, "$game_party.leader.equips[4] == $data_armors[492]"])
      Comment(["Shifting Core"])
      Script(["@r = rand(100)"])
      ConditionalBranch([12, "@r >= 98"])
        ConditionalBranch([12, "$game_party.leader.id >= 35 && $game_party.leader.id <= 39"])
          Script(["@ac = $game_party.leader.id"])
          Script(["@name = $game_actors[@ac].name"])
          Script(["@prog = 2250"])
          ScriptMore(["while @prog <= 2750"])
          ScriptMore(["if @name == $game_variables[@prog][2]"])
          ScriptMore(["break"])
          ScriptMore(["else"])
          ScriptMore(["@prog += 1"])
          ScriptMore(["end"])
          ScriptMore(["end"])
          ShowAnimation([-1, 147, true])
          ShowTextAttributes(["", 0, 0, 2])
          ShowText(["Thanks to the \\i[997] Shifting Core,\\. your Progeny has been granted \\i[681]!"])
          Script(["$game_variables[@prog][55] = 5"])
          Comment(["#####################"])
          Script(["$PU_AUTOMATIC_POPUP = false"])
          Script(["$game_actors[@ac].change_equip(4,$data_nil)"])
          ChangeArmor([492, 1, 0, 1, false])
          Script(["$PU_AUTOMATIC_POPUP = true"])
          Comment(["#####################"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_party.leader.hp <= 1"])
      ControlSwitches([666, 666, 0])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  Label(["freezeRES"])
  Empty([])
