CommonEvent 141
Name = "ADULT Gif resist"

  Script(["@r = 0"])
  Script(["@w = 0"])
  Script(["@resist = 0"])
  Script(["@lust = 0"])
  ScriptMore(["@corr = 0"])
  Script(["@add = 0"])
  Comment(["-------"])
  Comment(["resist"])
  Script(["@resist = 3"])
  ConditionalBranch([12, "$game_party.leader.id >= 35 && $game_party.leader.id <= 39 && $game_party.leader.class_id < 50"])
    Script(["@add = 3 + rand(3)"])
    Empty([])

  Else([])
    Script(["@lust=$game_party.members[0].parent_stat(1)"])
    ConditionalBranch([12, "$game_variables[116] == \"TaintedLove\""])
      Script(["@lust+=$game_party.members[0].parent_stat(0)"])
      Empty([])

    BranchEnd([])
    Script(["@corr=$game_party.members[0].parent_stat(4)"])
    Script(["@add = @lust + @corr + rand(2)"])
    Empty([])

  BranchEnd([])
  Script(["@resist += @add"])
  Comment(["-------"])
  ConditionalBranch([0, 792, 0])
    Script(["@resist = 10"])
    Empty([])

  BranchEnd([])
  Comment(["-------"])
  ConditionalBranch([0, 1198, 0])
    JumpToLabel(["end"])
    Empty([])

  BranchEnd([])
  ControlVariables([908, 908, 0, 2, 1, 2])
  Loop([])
    Wait([7])
    Script(["@w += 7"])
    ConditionalBranch([1, 908, 0, 1, 0])
      ConditionalBranch([12, "WolfPad.press?(:LEFT,0) || WolfPad.press?(:L_LEFT,0) || WolfPad.press?(:LEFT,1) || WolfPad.press?(:L_LEFT,1)"])
        JumpToLabel(["L"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([11, 4])
        Label(["L"])
        PlaySE(["RPG::SE(@name=\"Blow1\", @pitch=90, @volume=80)"])
        ShakeScreen([3, 4, 30, false])
        ControlVariables([908, 908, 0, 0, 2])
        ConditionalBranch([0, 2932, 0])
          Comment(["###########"])
          JumpToLabel(["DMGenemy"])
          Comment(["###########"])
          ConditionalBranch([12, "@r > 0"])
            Script(["@r -= 1"])
            Empty([])

          BranchEnd([])
          Empty([])

        Else([])
          Script(["@r += 1"])
          ConditionalBranch([12, "@r >= @resist"])
            BreakLoop([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    Else([])
      ConditionalBranch([12, "WolfPad.press?(:RIGHT,0) || WolfPad.press?(:L_RIGHT,0) || WolfPad.press?(:RIGHT,1) || WolfPad.press?(:L_RIGHT,1)"])
        JumpToLabel(["R"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([11, 6])
        Label(["R"])
        PlaySE(["RPG::SE(@name=\"Blow1\", @pitch=90, @volume=80)"])
        ShakeScreen([3, 4, 30, false])
        ControlVariables([908, 908, 0, 0, 1])
        ConditionalBranch([0, 2932, 0])
          Comment(["#######################"])
          Label(["DMGenemy"])
          Comment(["#######################"])
          ConditionalBranch([12, "$game_variables[6430] != 0"])
            Comment(["Whip Damage"])
            Script(["@id = $game_variables[6430]"])
            ConditionalBranch([12, "$game_party.leader.class_id == 176"])
              Comment(["Vashti Skill"])
              Script(["@lust = $game_party.members[0].parent_stat(1)"])
              ScriptMore(["@lust += 2"])
              ConditionalBranch([12, "$game_party.leader.equips[4] == $data_armors[670]"])
                Comment(["Zeal Toy"])
                Script(["@lust += 2"])
                Empty([])

              BranchEnd([])
              ConditionalBranch([12, "$game_variables[116] == \"TaintedLove\""])
                Script(["@lust += $game_party.members[0].parent_stat(0)"])
                Empty([])

              BranchEnd([])
              Script(["@lust *= 10"])
              Script(["@val = @lust * @lust"])
              Empty([])

            Else([])
              Script(["@lust = $game_party.members[0].parent_stat(1)"])
              ScriptMore(["@corr = $game_party.members[0].parent_stat(4)"])
              ScriptMore(["@lust += 1"])
              ScriptMore(["@corr += 1"])
              ConditionalBranch([12, "$game_variables[116] == \"TaintedLove\""])
                Script(["@lust += $game_party.members[0].parent_stat(0)"])
                Empty([])

              BranchEnd([])
              ConditionalBranch([12, "$game_party.leader.equips[4] == $data_armors[670]"])
                Comment(["Zeal Toy"])
                Script(["@lust += 2"])
                Empty([])

              BranchEnd([])
              Script(["@lust *= 5"])
              ScriptMore(["@corr *= 5"])
              Script(["@val = @lust * @corr"])
              Empty([])

            BranchEnd([])
            Script(["@dmg = 10 + @val * (rand(15)+85) / 100"])
            ConditionalBranch([12, "$game_party.leader.state?(169) == true"])
              Comment(["Sweet Scent"])
              Script(["@dmg *= 2"])
              Empty([])

            BranchEnd([])
            Script(["@hp = $game_map.events[@id].enemy.hp rescue -1"])
            ConditionalBranch([12, "@hp > 0"])
              Script(["puts(\"SUCC DMG = \" + @dmg.to_s)"])
              ScriptMore(["$game_map.events[@id].damageSec_enemy(@dmg)"])
              Empty([])

            BranchEnd([])
            Comment(["Whip Damage - END"])
            Empty([])

          BranchEnd([])
          Comment(["#######################"])
          ConditionalBranch([12, "@r > 0"])
            Script(["@r -= 1"])
            Empty([])

          BranchEnd([])
          Empty([])

        Else([])
          Script(["@r += 1"])
          ConditionalBranch([12, "@r >= @resist"])
            BreakLoop([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Comment(["timing"])
    ConditionalBranch([0, 2932, 0])
      ConditionalBranch([12, "@w > 30"])
        Script(["@w = 0"])
        Script(["@r += 1"])
        ConditionalBranch([12, "@r >= @resist"])
          BreakLoop([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    Else([])
      ConditionalBranch([12, "@r > 0 && @w > 30"])
        Script(["@r -= 1"])
        Script(["@w = 0"])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  RepeatAbove([])
  ErasePicture([2])
  ErasePicture([3])
  ControlSwitches([1197, 1197, 0])
  Script(["map_effects.set_zoom(100, 45)"])
  ErasePicture([2])
  ErasePicture([3])
  Label(["end"])
  Empty([])
