CommonEvent 459
Name = "★👤★ * Dates Trigg *"

  Comment(["######################"])
  Script(["@heatR = 0"])
  ConditionalBranch([0, 4156, 0])
    Comment(["All Res"])
    Script(["@heatR = 1"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_actors[1].state?(71) == true "])
    Comment(["Cool Aura Skill"])
    Script(["@heatR = 1"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_actors[1].state?(50) == true "])
    Comment(["COOL drinks"])
    Script(["@heatR = 1"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_actors[1].equips.include?$data_armors[267]"])
    Comment(["CREAM"])
    Script(["@heatR = 1"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "RTime::month == 6 && RTime::day >= 29"])
    ControlSwitches([774, 774, 0])
    Empty([])

  Else([])
    ConditionalBranch([12, "RTime::month == 7 && RTime::day <= 13"])
      ControlSwitches([774, 774, 0])
      Empty([])

    Else([])
      ControlSwitches([774, 774, 1])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  Comment(["##########################"])
  Script(["@wei = [0,52,64,83,330,40,18,65,7,40,90,58,61,46,77,64,270,55,1000,15,525,110,55,"])
  ScriptMore(["43,0,0]"])
  Comment(["##########################"])
  Comment(["Hot Spring"])
  Script(["@mapEr = [541,37,505,314,261,543,496]"])
  ScriptMore(["@i = 0"])
  ScriptMore(["while @i < @mapEr.length"])
  ScriptMore(["if $game_map.map_id == @mapEr[@i]"])
  ScriptMore([" @wei = Array.new(26,9999)"])
  ScriptMore(["end"])
  ScriptMore(["@i += 1"])
  ScriptMore(["end"])
  Comment(["##########################"])
  Comment(["### DP1 ###"])
  Script(["@pre = 0"])
  ConditionalBranch([0, 935, 0])
    ConditionalBranch([1, 741, 0, 50000, 1])
      ConditionalBranch([4, 18, 5, 192])
        ConditionalBranch([0, 413, 1])
          ConditionalBranch([0, 523, 1])
            ConditionalBranch([12, "$game_variables[1794] <= 2 || $game_switches[774] == true"])
              Script(["$game_self_switches[[236,80,\"A\"]] = true"])
              Script(["@pre = 1"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[236,80,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([0, 935, 0])
    ConditionalBranch([1, 741, 0, 50000, 1])
      ConditionalBranch([12, "@heatR == 1"])
        ConditionalBranch([12, "$game_actors[18].equips[5].nil?"])
          Script(["$game_self_switches[[496,74,\"A\"]] = true"])
          Script(["@pre = 1"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[496,74,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### DP2 ###"])
  Script(["@pre = 0"])
  ConditionalBranch([0, 2198, 0])
    ConditionalBranch([0, 2201, 0])
      ConditionalBranch([4, 11, 5, 116])
        ConditionalBranch([0, 413, 1])
          ConditionalBranch([0, 523, 1])
            Script(["$game_self_switches[[346,27,\"A\"]] = true"])
            Script(["@pre = 1"])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[346,27,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([0, 2198, 0])
    ConditionalBranch([0, 2201, 0])
      ConditionalBranch([12, "$game_actors[11].equips[5].nil?"])
        Script(["$game_self_switches[[541,16,\"A\"]] = true"])
        Script(["@pre = 1"])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[541,16,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### KOB ###"])
  Script(["@altFormKob = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 1"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore([" @altFormKob= 1"])
  ScriptMore(["elsif @succ == 2 "])
  ScriptMore([" @altFormKob = 1"])
  ScriptMore(["elsif $game_switches[2356] == true #dra form"])
  ScriptMore([" @altFormKob = 1"])
  ScriptMore(["elsif $game_switches[4120] == true #kobold dog"])
  ScriptMore([" @altFormKob = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore([" @altFormKob = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[2225] == true "])
  ScriptMore([" @altFormKob = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormKob == 0"])
    ConditionalBranch([4, 2, 5, 102])
      ConditionalBranch([12, "$game_actors[2].parent_stat(0) >= 4"])
        ConditionalBranch([0, 413, 1])
          ConditionalBranch([0, 523, 1])
            ConditionalBranch([12, "$game_variables[1794] <= 2 || $game_switches[774] == true"])
              Comment(["Spring/Summer - Summerfest"])
              Script(["$game_self_switches[[236,4,\"A\"]] = true"])
              Script(["@pre = 1"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[236,4,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormKob == 0"])
    ConditionalBranch([12, "$game_actors[2].equips[5].nil?"])
      ConditionalBranch([12, "$game_actors[2].parent_stat(0) >= 4"])
        ConditionalBranch([12, "$game_actors[2].parent_stat(1) >= 4"])
          ConditionalBranch([0, 413, 1])
            ConditionalBranch([0, 522, 0])
              Script(["$game_self_switches[[37,11,\"A\"]] = true"])
              Script(["@pre = 1"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[37,11,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### HUM ###"])
  Script(["@altFormHum = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 2"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore([" @altFormHum = 1"])
  ScriptMore(["elsif @succ == 2 "])
  ScriptMore([" @altFormHum = 1"])
  ScriptMore(["elsif $game_switches[4116] == true #porgy hum"])
  ScriptMore([" @altFormHum = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore([" @altFormHum = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[2226] == true "])
  ScriptMore([" @altFormHum = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormHum == 0"])
    ConditionalBranch([4, 5, 5, 103])
      ConditionalBranch([12, "$game_actors[5].parent_stat(0) >= 4"])
        ConditionalBranch([0, 413, 1])
          ConditionalBranch([0, 523, 1])
            ConditionalBranch([12, "$game_variables[1794] <= 2 || $game_switches[774] == true"])
              Comment(["Spring/Summer - Summerfest"])
              Script(["$game_self_switches[[236,41,\"A\"]] = true"])
              Script(["@pre = 1"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[236,41,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormHum == 0"])
    ConditionalBranch([12, "$game_actors[5].equips[5].nil?"])
      ConditionalBranch([12, "$game_actors[5].parent_stat(0) >= 4"])
        ConditionalBranch([12, "$game_actors[5].parent_stat(1) >= 4"])
          Script(["$game_self_switches[[314,3,\"A\"]] = true"])
          Script(["@pre = 1"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[314,3,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### SLI ###"])
  Script(["  @altFormSli = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 3"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore(["  @altFormSli = 1"])
  ScriptMore(["elsif @succ == 2 "])
  ScriptMore(["  @altFormSli = 1"])
  ScriptMore(["elsif $game_switches[2445] == true #Ooz form"])
  ScriptMore(["  @altFormSli = 1  "])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore(["  @altFormSli = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[2227] == true"])
  ScriptMore(["  @altFormSli = 1"])
  ScriptMore(["end "])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormSli == 0"])
    ConditionalBranch([4, 7, 5, 104])
      ConditionalBranch([12, "$game_actors[7].parent_stat(0) >= 4"])
        ConditionalBranch([0, 413, 1])
          ConditionalBranch([0, 523, 1])
            ConditionalBranch([12, "$game_variables[1794] <= 2 || $game_switches[774] == true"])
              Comment(["Spring/Summer - Summerfest"])
              Script(["$game_self_switches[[52,132,\"A\"]] = true"])
              Script(["@pre = 1"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[52,132,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormSli == 0"])
    ConditionalBranch([12, "$game_actors[7].equips[5].nil?"])
      ConditionalBranch([12, "$game_actors[7].parent_stat(0) >= 4"])
        ConditionalBranch([12, "$game_actors[7].parent_stat(1) >= 4"])
          Script(["$game_self_switches[[261,46,\"A\"]] = true"])
          Script(["@pre = 1"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[261,46,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### GOL ###"])
  Script([" @altFormGol = 0"])
  ScriptMore(["#succ check"])
  ScriptMore([" @sch = 4 "])
  ScriptMore([" @thiccLim = @wei[@sch]"])
  ScriptMore([" @succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore([" @prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore([" @altFormGol = 1"])
  ScriptMore(["elsif @succ == 2 "])
  ScriptMore([" @altFormGol = 1"])
  ScriptMore(["elsif $game_switches[2450] == true #Rock"])
  ScriptMore([" @altFormGol = 1"])
  ScriptMore(["elsif $game_switches[2452] == true #Human"])
  ScriptMore([" @altFormGol = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore([" @altFormGol = 1   "])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[2228] == true"])
  ScriptMore([" @altFormGol = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormGol == 0"])
    ConditionalBranch([4, 6, 5, 105])
      ConditionalBranch([12, "$game_actors[6].parent_stat(0) >= 4"])
        ConditionalBranch([0, 1571, 0])
          Script(["$game_self_switches[[409,8,\"A\"]] = true"])
          Script(["@pre = 1"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[409,8,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  Comment(["CHECK HEAT RES"])
  ConditionalBranch([12, "@altFormGol == 0"])
    ConditionalBranch([12, "$game_actors[6].parent_stat(0) >= 4"])
      ConditionalBranch([12, "$game_actors[6].parent_stat(1) >= 4"])
        ConditionalBranch([12, "@heatR == 1"])
          ConditionalBranch([12, "$game_actors[6].equips[5].nil?"])
            Script(["$game_self_switches[[496,73,\"A\"]] = true"])
            Script(["@pre = 1"])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[496,73,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### INS ###"])
  Script([" @altFormIns = 0"])
  ScriptMore(["#succ check"])
  ScriptMore([" @sch = 5"])
  ScriptMore([" @thiccLim = @wei[@sch]"])
  ScriptMore([" @succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore([" @prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore([" @altFormIns = 1"])
  ScriptMore(["elsif @succ == 2 "])
  ScriptMore([" @altFormIns = 1"])
  ScriptMore(["elsif $game_actors[8].state?(29) == true #Hornet"])
  ScriptMore([" @altFormIns = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore([" @altFormIns = 1   "])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[2229] == true"])
  ScriptMore([" @altFormIns = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormIns == 0"])
    ConditionalBranch([4, 8, 5, 106])
      ConditionalBranch([12, "$game_actors[8].parent_stat(0) >= 4"])
        ConditionalBranch([0, 413, 1])
          ConditionalBranch([0, 523, 1])
            ConditionalBranch([12, "$game_variables[1794] <= 2 || $game_switches[774] == true"])
              Comment(["Spring/Summer - Summerfest"])
              Script(["$game_self_switches[[236,72,\"A\"]] = true"])
              Script(["@pre = 1"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[236,72,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormIns == 0"])
    ConditionalBranch([0, 1650, 0])
      ConditionalBranch([12, "$game_actors[8].parent_stat(0) >= 4"])
        ConditionalBranch([12, "$game_actors[8].parent_stat(1) >= 4"])
          Script(["$game_self_switches[[541,12,\"A\"]] = true"])
          Script(["@pre = 1"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[541,12,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### SKE ###"])
  Script(["@altFormSke = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 6"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore([" @altFormSke = 1"])
  ScriptMore(["elsif @succ == 2 "])
  ScriptMore([" @altFormSke = 1"])
  ScriptMore(["elsif $game_switches[2393] == true #alt form"])
  ScriptMore([" @altFormSke = 1"])
  ScriptMore(["elsif $game_switches[2395] == true #alt form"])
  ScriptMore([" @altFormSke = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore([" @altFormSke = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[2230] == true "])
  ScriptMore([" @altFormSke = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormSke == 0"])
    ConditionalBranch([4, 9, 5, 107])
      ConditionalBranch([12, "$game_actors[9].parent_stat(0) >= 4"])
        ConditionalBranch([0, 413, 1])
          ConditionalBranch([0, 523, 1])
            ConditionalBranch([12, "$game_variables[1794] <= 2 || $game_switches[774] == true"])
              Comment(["Spring/Summer - Summerfest"])
              Script(["$game_self_switches[[236,42,\"A\"]] = true"])
              Script(["@pre = 1"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[236,42,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormSke == 0"])
    ConditionalBranch([12, "$game_actors[9].equips[5].nil?"])
      ConditionalBranch([12, "$game_actors[9].parent_stat(0) >= 4"])
        ConditionalBranch([12, "$game_actors[9].parent_stat(1) >= 4"])
          Script(["$game_self_switches[[543,11,\"A\"]] = true"])
          Script(["@pre = 1"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[543,11,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### GHO ###"])
  Script(["@altFormGho = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 8"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore(["  @altFormGho = 1"])
  ScriptMore(["elsif $game_switches[2454] == true #human form"])
  ScriptMore(["  @altFormGho = 1"])
  ScriptMore(["elsif $game_switches[3050] == true #yokai form"])
  ScriptMore(["  @altFormGho = 1 "])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore(["  @altFormGho = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[2232] == true"])
  ScriptMore(["  @altFormGho = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormGho == 0"])
    ConditionalBranch([4, 12, 5, 109])
      ConditionalBranch([12, "$game_actors[12].parent_stat(0) >= 4"])
        ConditionalBranch([0, 413, 0])
          ConditionalBranch([0, 523, 1])
            Comment(["Spring/Summer"])
            Script(["$game_self_switches[[346,26,\"A\"]] = true"])
            Script(["@pre = 1"])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[346,26,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormGho == 0"])
    ConditionalBranch([12, "$game_actors[12].equips[5].nil?"])
      ConditionalBranch([12, "$game_actors[12].parent_stat(0) >= 4"])
        ConditionalBranch([12, "$game_actors[12].parent_stat(1) >= 4"])
          Script(["$game_self_switches[[261,48,\"A\"]] = true"])
          Script(["@pre = 1"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[261,48,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### GOB ###"])
  Script(["@altFormGob = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 9"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore([" @altFormGob = 1"])
  ScriptMore(["elsif @succ == 2 "])
  ScriptMore([" @altFormGob = 1"])
  ScriptMore(["elsif $game_actors[14].state?(28) == true #alt form"])
  ScriptMore([" @altFormGob = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore([" @altFormGob = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[2233] == true "])
  ScriptMore([" @altFormGob = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormGob == 0"])
    ConditionalBranch([12, "$game_actors[14].equips[5] == $data_armors[649] || $game_actors[14].equips[5] == $data_armors[110]"])
      ConditionalBranch([12, "$game_actors[14].parent_stat(0) >= 4"])
        ConditionalBranch([0, 413, 1])
          ConditionalBranch([0, 523, 1])
            ConditionalBranch([12, "$game_variables[1794] <= 2 || $game_switches[774] == true"])
              Comment(["Spring/Summer - Summerfest"])
              Script(["$game_self_switches[[236,43,\"A\"]] = true"])
              Script(["@pre = 1"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[236,43,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormGob == 0"])
    ConditionalBranch([12, "$game_actors[14].equips[5].nil?"])
      ConditionalBranch([12, "$game_actors[14].parent_stat(0) >= 4"])
        ConditionalBranch([12, "$game_actors[14].parent_stat(1) >= 4"])
          Comment(["Hot Springs"])
          Script(["$game_self_switches[[314,35,\"A\"]] = true"])
          Script(["@pre = 1"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[314,35,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### MOT ###"])
  Script(["  @altFormMot = 0"])
  ScriptMore([" #succ check"])
  ScriptMore(["@sch = 10"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore([" #succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore(["@altFormMot = 1"])
  ScriptMore(["elsif @succ == 2 "])
  ScriptMore(["@altFormMot = 1"])
  ScriptMore(["elsif $game_switches[2460] == true #butterfly form"])
  ScriptMore(["@altFormMot = 1"])
  ScriptMore(["elsif $game_switches[4118] == true #fairy form"])
  ScriptMore(["@altFormMot = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore(["@altFormMot = 1   "])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[2234] == true"])
  ScriptMore(["@altFormMot = 1"])
  ScriptMore(["end  "])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormMot == 0"])
    ConditionalBranch([4, 21, 5, 111])
      ConditionalBranch([12, "$game_actors[21].parent_stat(0) >= 4"])
        ConditionalBranch([0, 413, 0])
          ConditionalBranch([0, 523, 1])
            ConditionalBranch([12, "$game_variables[1794] <= 2 || $game_switches[774] == true"])
              Comment(["Spring/Summer - Summerfest"])
              Script(["$game_self_switches[[236,33,\"A\"]] = true"])
              Script(["@pre = 1"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[236,33,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormMot == 0"])
    ConditionalBranch([12, "$game_actors[21].equips[5].nil?"])
      ConditionalBranch([12, "$game_actors[21].parent_stat(0) >= 4"])
        ConditionalBranch([12, "$game_actors[21].parent_stat(1) >= 4"])
          Script(["$game_self_switches[[541,15,\"A\"]] = true"])
          Script(["@pre = 1"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[541,15,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### CAT ###"])
  Script(["  @altFormCat = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 11"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore(["  @altFormCat = 1"])
  ScriptMore(["elsif @succ == 2 "])
  ScriptMore(["  @altFormCat = 1"])
  ScriptMore(["elsif $game_switches[2340] == true #mata form"])
  ScriptMore(["  @altFormCat = 1"])
  ScriptMore(["elsif $game_switches[4115] == true #fish form"])
  ScriptMore(["  @altFormCat = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore(["  @altFormCat = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[2235] == true"])
  ScriptMore(["  @altFormCat = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormCat == 0"])
    ConditionalBranch([4, 23, 5, 112])
      ConditionalBranch([12, "$game_actors[23].parent_stat(0) >= 4"])
        ConditionalBranch([0, 413, 1])
          ConditionalBranch([0, 523, 1])
            ConditionalBranch([12, "$game_variables[1794] <= 2 || $game_switches[774] == true"])
              Comment(["Spring/Summer - Summerfest"])
              Script(["$game_self_switches[[236,16,\"A\"]] = true"])
              Script(["@pre = 1"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[236,16,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormCat == 0"])
    ConditionalBranch([12, "$game_actors[23].equips[5].nil?"])
      ConditionalBranch([12, "$game_actors[23].parent_stat(0) >= 4"])
        ConditionalBranch([12, "$game_actors[23].parent_stat(1) >= 4"])
          Script(["$game_self_switches[[314,2,\"A\"]] = true"])
          Script(["@pre = 1"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[314,2,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### DOG ###"])
  Script(["@altFormDog = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 12"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore(["  @altFormDog = 1"])
  ScriptMore(["elsif @succ == 2 "])
  ScriptMore(["\t@altFormDog = 1"])
  ScriptMore(["elsif $game_switches[2338] == true #djinn form"])
  ScriptMore(["  @altFormDog = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore(["  @altFormDog = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[2236] == true"])
  ScriptMore(["  @altFormDog = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormDog == 0"])
    ConditionalBranch([4, 16, 5, 113])
      ConditionalBranch([12, "$game_actors[16].parent_stat(0) >= 4"])
        ConditionalBranch([0, 413, 1])
          ConditionalBranch([0, 523, 1])
            ConditionalBranch([12, "$game_variables[1794] <= 2 || $game_switches[774] == true"])
              Comment(["Spring/Summer - Summerfest"])
              Script(["$game_self_switches[[52,140,\"A\"]] = true"])
              Script(["@pre = 1"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[52,140,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormDog == 0"])
    ConditionalBranch([12, "$game_actors[16].equips[5].nil?"])
      ConditionalBranch([12, "$game_actors[16].parent_stat(0) >= 4"])
        ConditionalBranch([12, "$game_actors[16].parent_stat(1) >= 4"])
          Script(["$game_self_switches[[543,9,\"A\"]] = true"])
          Script(["@pre = 1"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[543,9,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### MOU ###"])
  Script(["@altFormMou = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 13"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore([" @altFormMou = 1"])
  ScriptMore(["elsif @succ == 2 "])
  ScriptMore([" @altFormMou = 1"])
  ScriptMore(["elsif $game_switches[4117] == true #alt form"])
  ScriptMore([" @altFormMou = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore([" @altFormMou = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[2237] == true "])
  ScriptMore([" @altFormMou = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormMou == 0"])
    ConditionalBranch([4, 26, 5, 153])
      ConditionalBranch([12, "$game_actors[26].parent_stat(0) >= 4"])
        ConditionalBranch([0, 413, 1])
          ConditionalBranch([0, 523, 1])
            Comment(["Spring/Summer - Summerfest"])
            Script(["$game_self_switches[[346,21,\"A\"]] = true"])
            Script(["@pre = 1"])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[346,21,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormMou == 0"])
    ConditionalBranch([12, "$game_actors[26].equips[5].nil?"])
      ConditionalBranch([12, "$game_actors[26].parent_stat(0) >= 4"])
        ConditionalBranch([12, "$game_actors[26].parent_stat(1) >= 4"])
          ConditionalBranch([0, 413, 1])
            ConditionalBranch([0, 522, 0])
              Script(["$game_self_switches[[37,12,\"A\"]] = true"])
              Script(["@pre = 1"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[37,12,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### RAB ###"])
  Script(["@altFormRab = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 14"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore([" @altFormRab = 1"])
  ScriptMore(["elsif @succ == 2 "])
  ScriptMore([" @altFormRab = 1"])
  ScriptMore(["elsif $game_switches[3014] == true #alt form"])
  ScriptMore([" @altFormRab = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore([" @altFormRab = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[2921] == true "])
  ScriptMore([" @altFormRab = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormRab == 0"])
    ConditionalBranch([4, 30, 5, 168])
      ConditionalBranch([12, "$game_actors[30].parent_stat(0) >= 4"])
        ConditionalBranch([0, 413, 1])
          ConditionalBranch([0, 523, 1])
            ConditionalBranch([12, "$game_variables[1794] <= 2 || $game_switches[774] == true"])
              Comment(["Spring/Summer - Summerfest"])
              Script(["$game_self_switches[[236,44,\"A\"]] = true"])
              Script(["@pre = 1"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[236,44,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormRab == 0"])
    ConditionalBranch([12, "$game_actors[30].equips[5].nil?"])
      ConditionalBranch([12, "$game_actors[30].parent_stat(0) >= 4"])
        ConditionalBranch([12, "$game_actors[30].parent_stat(1) >= 4"])
          Script(["$game_self_switches[[261,49,\"A\"]] = true"])
          Script(["@pre = 1"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[261,49,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### HAR ###"])
  Script(["@altFormHar = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 15"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore(["  @altFormHar = 1"])
  ScriptMore(["elsif @succ == 2 "])
  ScriptMore(["  @altFormHar = 1"])
  ScriptMore(["elsif $game_switches[4119] == true #wolf harP"])
  ScriptMore(["  @altFormHar = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore(["  @altFormHar = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[3314] == true"])
  ScriptMore(["  @altFormHar = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormHar == 0"])
    ConditionalBranch([4, 32, 5, 234])
      ConditionalBranch([12, "$game_actors[32].parent_stat(0) >= 4"])
        ConditionalBranch([0, 413, 1])
          ConditionalBranch([0, 523, 1])
            ConditionalBranch([12, "$game_variables[1794] <= 2 || $game_switches[774] == true"])
              Comment(["Spring/Summer - Summerfest"])
              Script(["$game_self_switches[[107,24,\"A\"]] = true"])
              Script(["@pre = 1"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[107,24,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormHar == 0"])
    ConditionalBranch([12, "$game_actors[32].equips[5].nil?"])
      ConditionalBranch([12, "$game_actors[32].parent_stat(0) >= 4"])
        ConditionalBranch([12, "$game_actors[32].parent_stat(1) >= 4"])
          Script(["$game_self_switches[[543,10,\"A\"]] = true"])
          Script(["@pre = 1"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[543,10,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### WYV ###"])
  Script(["@altFormWyv = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 16"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore(["  @altFormWyv = 1"])
  ScriptMore(["elsif @succ == 2 "])
  ScriptMore(["  @altFormWyv = 1"])
  ScriptMore(["elsif $game_switches[3896] == true #human wyvP"])
  ScriptMore(["  @altFormWyv = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore(["  @altFormWyv = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[3512] == true"])
  ScriptMore(["  @altFormWyv = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormWyv == 0"])
    ConditionalBranch([4, 41, 5, 246])
      ConditionalBranch([12, "$game_actors[41].parent_stat(0) >= 4"])
        ConditionalBranch([0, 413, 1])
          ConditionalBranch([0, 523, 1])
            ConditionalBranch([12, "$game_variables[1794] <= 2 || $game_switches[774] == true"])
              Comment(["Spring/Summer - Summerfest"])
              Script(["$game_self_switches[[52,143,\"A\"]] = true"])
              Script(["@pre = 1"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[52,143,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormWyv == 0"])
    ConditionalBranch([12, "$game_actors[41].parent_stat(0) >= 4"])
      ConditionalBranch([12, "$game_actors[41].parent_stat(1) >= 4"])
        ConditionalBranch([12, "@heatR == 1"])
          ConditionalBranch([12, "$game_actors[41].equips[5].nil?"])
            Script(["$game_self_switches[[496,71,\"A\"]] = true"])
            Script(["@pre = 1"])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[496,71,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### FIN ###"])
  Script(["@altFormFin = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 17"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore(["  @altFormFin = 1"])
  ScriptMore(["elsif @succ == 2 "])
  ScriptMore(["  @altFormFin = 1"])
  ScriptMore(["elsif $game_switches[4593] == true #bird finP"])
  ScriptMore(["  @altFormFin = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore(["  @altFormFin = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[4052] == true"])
  ScriptMore(["  @altFormFin = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormFin == 0"])
    ConditionalBranch([4, 46, 5, 306])
      ConditionalBranch([12, "$game_actors[46].parent_stat(0) >= 4"])
        ConditionalBranch([0, 1571, 0])
          Script(["$game_self_switches[[409,10,\"A\"]] = true"])
          Script(["@pre = 1"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[409,10,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormFin == 0"])
    ConditionalBranch([12, "$game_actors[46].equips[5].nil?"])
      ConditionalBranch([12, "$game_actors[46].parent_stat(0) >= 4"])
        ConditionalBranch([12, "$game_actors[46].parent_stat(1) >= 4"])
          Script(["$game_self_switches[[541,14,\"A\"]] = true"])
          Script(["@pre = 1"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[541,14,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### MER ###"])
  Script(["@altFormMer = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 18"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore(["  @altFormMer = 1"])
  ScriptMore(["elsif @succ == 2  "])
  ScriptMore(["  @altFormMer = 1"])
  ScriptMore(["elsif $game_switches[4595] == true #scylla merP"])
  ScriptMore(["  @altFormMer = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore(["  @altFormMer = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[4232] == true"])
  ScriptMore(["  @altFormMer = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormMer == 0"])
    ConditionalBranch([4, 48, 5, 359])
      ConditionalBranch([12, "$game_actors[48].parent_stat(0) >= 4"])
        ConditionalBranch([0, 1571, 0])
          ConditionalBranch([12, "$game_variables[6510] >= 2"])
            Script(["$game_self_switches[[139,31,\"A\"]] = true"])
            Script(["@pre = 1"])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[139,31,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormMer == 0"])
    ConditionalBranch([12, "$game_actors[48].equips[5].nil?"])
      ConditionalBranch([12, "$game_actors[48].parent_stat(0) >= 4"])
        ConditionalBranch([12, "$game_actors[48].parent_stat(1) >= 4"])
          Script(["$game_self_switches[[314,39,\"A\"]] = true"])
          Script(["@pre = 1"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[314,39,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### BIR ###"])
  Script(["@altFormBir = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 19"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore(["  @altFormBir = 1"])
  ScriptMore(["elsif @succ == 2 "])
  ScriptMore(["\t@altFormBir = 1"])
  ScriptMore(["elsif $game_switches[4592] == true #finhead birdP"])
  ScriptMore(["  @altFormBir = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore(["  @altFormBir = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[4386] == true"])
  ScriptMore(["  @altFormBir = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormBir == 0"])
    ConditionalBranch([4, 53, 5, 387])
      ConditionalBranch([12, "$game_actors[53].parent_stat(0) >= 4"])
        ConditionalBranch([0, 413, 1])
          ConditionalBranch([0, 523, 1])
            ConditionalBranch([12, "$game_variables[1794] <= 2 || $game_switches[774] == true"])
              Comment(["Spring/Summer - Summerfest"])
              Script(["$game_self_switches[[236,39,\"A\"]] = true"])
              Script(["@pre = 1"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[236,39,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormBir == 0"])
    ConditionalBranch([12, "$game_actors[53].equips[5].nil?"])
      ConditionalBranch([12, "$game_actors[53].parent_stat(0) >= 4"])
        ConditionalBranch([12, "$game_actors[53].parent_stat(1) >= 4"])
          Script(["$game_self_switches[[543,6,\"A\"]] = true"])
          Script(["@pre = 1"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[543,6,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### ONI ###"])
  Script(["@altFormOni = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 20"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore(["  @altFormOni = 1"])
  ScriptMore(["elsif @succ == 2  "])
  ScriptMore(["  @altFormOni = 1"])
  ScriptMore(["elsif $game_switches[4613] == true #raijin oniP"])
  ScriptMore(["  @altFormOni = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore(["  @altFormOni = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[4687] == true"])
  ScriptMore(["  @altFormOni = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormOni == 0"])
    ConditionalBranch([4, 54, 5, 516])
      ConditionalBranch([12, "$game_actors[54].parent_stat(0) >= 4"])
        ConditionalBranch([0, 413, 1])
          ConditionalBranch([0, 523, 1])
            ConditionalBranch([12, "$game_variables[1794] <= 2 || $game_switches[774] == true"])
              Comment(["Spring/Summer - Summerfest"])
              Script(["$game_self_switches[[236,75,\"A\"]] = true"])
              Script(["@pre = 1"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[236,75,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  Comment(["CHECK HEAT RES"])
  ConditionalBranch([12, "@altFormOni == 0"])
    ConditionalBranch([12, "$game_actors[54].parent_stat(0) >= 4"])
      ConditionalBranch([12, "$game_actors[54].parent_stat(1) >= 4"])
        ConditionalBranch([12, "@heatR == 1"])
          ConditionalBranch([12, "$game_actors[54].equips[5].nil?"])
            Script(["$game_self_switches[[505,19,\"A\"]] = true"])
            Script(["@pre = 1"])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[505,19,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### PLA ###"])
  Script(["@altFormPla = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 21"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore(["  @altFormPla = 1"])
  ScriptMore(["elsif @succ == 2  "])
  ScriptMore(["  @altFormPla = 1"])
  ScriptMore(["elsif $game_switches[3207] == true #thorn PlaP"])
  ScriptMore(["  @altFormPla = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore(["  @altFormPla = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[3133] == true"])
  ScriptMore(["  @altFormPla = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormPla == 0"])
    ConditionalBranch([4, 56, 5, 581])
      ConditionalBranch([12, "$game_actors[56].parent_stat(0) >= 4"])
        ConditionalBranch([0, 1571, 0])
          Script(["@pre = 1"])
          Script(["$game_self_switches[[409,58,\"A\"]] = true"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[409,58,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormPla == 0"])
    ConditionalBranch([12, "$game_actors[56].parent_stat(0) >= 4"])
      ConditionalBranch([12, "$game_actors[56].parent_stat(1) >= 4"])
        ConditionalBranch([12, "$game_actors[56].equips[5].nil?"])
          Script(["@pre = 1"])
          Script(["$game_self_switches[[261,45,\"A\"]] = true"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[261,45,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### SHA ###"])
  Script(["@altFormSha = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 22"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore(["  @altFormSha = 1"])
  ScriptMore(["elsif @succ == 2  "])
  ScriptMore(["  @altFormSha = 1"])
  ScriptMore(["elsif $game_switches[2840] == true #Shadow Form"])
  ScriptMore(["  @altFormSha = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore(["  @altFormSha = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[2813] == true"])
  ScriptMore(["  @altFormSha = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormSha == 0"])
    ConditionalBranch([4, 57, 5, 761])
      ConditionalBranch([12, "$game_actors[57].parent_stat(0) >= 4"])
        ConditionalBranch([0, 413, 1])
          ConditionalBranch([0, 523, 1])
            ConditionalBranch([12, "$game_variables[1794] <= 2 || $game_switches[774] == true"])
              Comment(["Spring/Summer - Summerfest"])
              Script(["@pre = 1"])
              Script(["$game_self_switches[[107,25,\"A\"]] = true"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[107,25,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormSha == 0"])
    ConditionalBranch([12, "$game_actors[57].equips[5].nil?"])
      ConditionalBranch([12, "$game_actors[57].parent_stat(0) >= 4"])
        ConditionalBranch([12, "$game_actors[57].parent_stat(1) >= 4"])
          Script(["@pre = 1"])
          Script(["$game_self_switches[[314,38,\"A\"]] = true"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[314,38,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Comment(["### DES ###"])
  Script(["  @altFormDes = 0"])
  ScriptMore(["#succ check"])
  ScriptMore(["@sch = 7"])
  ScriptMore(["@thiccLim = @wei[@sch]"])
  ScriptMore(["@succ = $game_variables[6401][@sch] rescue -1"])
  ScriptMore(["@prince = $game_variables[6406][@sch] rescue -1"])
  ScriptMore(["#succ check"])
  ScriptMore(["if $game_switches[3959] == true || @prince == 2 #pri form"])
  ScriptMore(["  @altFormDes = 1"])
  ScriptMore(["elsif $game_switches[4121] == true #djinn form"])
  ScriptMore(["  @altFormDes = 1"])
  ScriptMore(["elsif $game_variables[6202][@sch] >= @thiccLim"])
  ScriptMore(["  @altFormDes = 1"])
  ScriptMore(["end  "])
  ScriptMore(["if $game_switches[2231] == true"])
  ScriptMore(["  @altFormDes = 1"])
  ScriptMore(["end"])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormDes == 0"])
    ConditionalBranch([4, 4, 5, 108])
      ConditionalBranch([12, "$game_actors[4].parent_stat(0) >= 4"])
        ConditionalBranch([0, 413, 1])
          ConditionalBranch([0, 523, 1])
            Comment(["Spring/Summer"])
            Script(["$game_self_switches[[107,23,\"A\"]] = true"])
            Script(["@pre = 1"])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[107,23,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Script(["@pre = 0"])
  ConditionalBranch([12, "@altFormDes == 0"])
    ConditionalBranch([12, "$game_actors[4].equips[5].nil?"])
      ConditionalBranch([12, "$game_actors[4].parent_stat(0) >= 4"])
        ConditionalBranch([12, "$game_actors[4].parent_stat(1) >= 4"])
          ConditionalBranch([0, 413, 1])
            ConditionalBranch([0, 522, 0])
              Script(["$game_self_switches[[37,8,\"A\"]] = true"])
              Script(["@pre = 1"])
              Empty([])

            BranchEnd([])
            Empty([])

          BranchEnd([])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@pre == 0"])
    Script(["$game_self_switches[[37,8,\"A\"]] = false"])
    Empty([])

  BranchEnd([])
  Empty([])
