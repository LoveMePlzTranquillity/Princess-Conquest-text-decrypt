CommonEvent 143
Name = "★ sperm gain"

  ControlVariables([983, 983, 0, 2, 1, 3])
  Script(["@str = $game_variables[393]"])
  ConditionalBranch([12, "@str.include?\"Bat\""])
    Label(["bat"])
    Comment(["Bats"])
    ChangeItems([130, 0, 1, 983])
    ConditionalBranch([0, 1276, 0])
      ConditionalBranch([12, "objective_complete?(13, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 130, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(13,0)"])
          ScriptMore(["call_quest_journal(13)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str.include?\"Skullwidow\""])
    JumpToLabel(["spider"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str.include?\"spiderling\""])
    JumpToLabel(["spider"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str.include?\"Webling\""])
    Comment(["Weblings, Spiderlings, SkullW"])
    Label(["spider"])
    ChangeItems([131, 0, 1, 983])
    ConditionalBranch([0, 1276, 0])
      ConditionalBranch([12, "objective_complete?(13, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 131, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(13,0)"])
          ScriptMore(["call_quest_journal(13)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393][1] == \"S\" && $game_variables[393][2] == \"c\" && $game_variables[393][3] == \"a\" && $game_variables[393][4] == \"r\" && $game_variables[393][5] == \"a\""])
    JumpToLabel(["scarab"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393] == \"$Scarab\" || $game_variables[393] == \"$ScarabElite\""])
    Label(["scarab"])
    Comment(["Scarabs"])
    ChangeItems([132, 0, 1, 983])
    ConditionalBranch([0, 1276, 0])
      ConditionalBranch([12, "objective_complete?(13, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 132, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(13,0)"])
          ScriptMore(["call_quest_journal(13)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393][1] == \"W\" && $game_variables[393][2] == \"o\" && $game_variables[393][3] == \"l\""])
    JumpToLabel(["wolf"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393] == \"$Wolf%(4)\""])
    Label(["wolf"])
    Comment(["Wolves"])
    ChangeItems([133, 0, 1, 983])
    ConditionalBranch([0, 1276, 0])
      ConditionalBranch([12, "objective_complete?(13, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 133, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(13,0)"])
          ScriptMore(["call_quest_journal(13)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393][1] == \"K\" && $game_variables[393][2] == \"o\" && $game_variables[393][3] == \"b\" && $game_variables[393][7] == \"Z\""])
    JumpToLabel(["curse"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393][1] == \"W\" && $game_variables[393][2] == \"e\" && $game_variables[393][3] == \"n\""])
    JumpToLabel(["curse"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393] == \"$KoboldZom\" || $game_variables[393] == \"$Wendigo\""])
    Label(["curse"])
    Comment(["Necrom Kobs"])
    ChangeItems([135, 0, 1, 983])
    ConditionalBranch([0, 1276, 0])
      ConditionalBranch([12, "objective_complete?(13, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 135, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(13,0)"])
          ScriptMore(["call_quest_journal(13)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393][1] == \"L\" && $game_variables[393][2] == \"i\" && $game_variables[393][3] == \"t\""])
    JumpToLabel(["snake"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393] == \"$057-Snake03\" || $game_variables[393] == \"$LittleSandSnake%(4)\""])
    Label(["snake"])
    Comment(["Snakes"])
    ChangeItems([136, 0, 1, 983])
    ConditionalBranch([0, 1276, 0])
      ConditionalBranch([12, "objective_complete?(13, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 136, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(13,0)"])
          ScriptMore(["call_quest_journal(13)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str.include?\"Grenilla\""])
    JumpToLabel(["frog"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str.include?\"Velenophagus\""])
    JumpToLabel(["frog"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393] == \"$Velenophagus\""])
    Label(["frog"])
    Comment(["Frogs"])
    ChangeItems([138, 0, 1, 983])
    ConditionalBranch([0, 1276, 0])
      ConditionalBranch([12, "objective_complete?(13, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 138, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(13,0)"])
          ScriptMore(["call_quest_journal(13)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str.include?\"$Knight\""])
    JumpToLabel(["human"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393] == \"$Knight\""])
    Label(["human"])
    Comment(["Humans"])
    ChangeItems([142, 0, 1, 983])
    ConditionalBranch([0, 2054, 0])
      ConditionalBranch([12, "objective_complete?(33, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 142, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(33,0)"])
          ScriptMore(["call_quest_journal(33)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393][1] == \"J\" && $game_variables[393][2] == \"e\" && $game_variables[393][3] == \"l\""])
    JumpToLabel(["slime"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393] == \"$Jell\" || $game_variables[393] == \"$JellP\" || $game_variables[393] == \"$JellR\" || $game_variables[393] == \"$JellG\" || $game_variables[393] == \"$JellB\" || $game_variables[393] == \"$Jello%(4)\" || $game_variables[393] == \"$Jello%(4)\" || $game_variables[393] == \"$CrysJello%(4)\" || $game_variables[393] == \"$IceJello%(4)\""])
    Label(["slime"])
    Comment(["Slimes"])
    ChangeItems([143, 0, 1, 983])
    ConditionalBranch([0, 2054, 0])
      ConditionalBranch([12, "objective_complete?(33, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 143, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(33,0)"])
          ScriptMore(["call_quest_journal(33)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str.include?\"$RockSma\""])
    JumpToLabel(["golem"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str.include?\"$RockEle\""])
    JumpToLabel(["golem"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str.include?\"$MossEle\""])
    JumpToLabel(["golem"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str.include?\"$Molten\""])
    JumpToLabel(["golem"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str.include?\"Gol\""])
    JumpToLabel(["golem"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str.include?\"gol\""])
    JumpToLabel(["golem"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393] == \"$Golem\" || $game_variables[393] == \"$golemboss\" || $game_variables[393] == \"$GolemBoss%(4)\""])
    Label(["golem"])
    Comment(["Golems"])
    ChangeItems([144, 0, 1, 983])
    ConditionalBranch([0, 2054, 0])
      ConditionalBranch([12, "objective_complete?(33, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 144, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(33,0)"])
          ScriptMore(["call_quest_journal(33)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  Script(["@str = $game_variables[393]"])
  ConditionalBranch([12, "@str[1] == \"I\" && @str[2] == \"n\" && @str[3] == \"s\""])
    JumpToLabel(["insect"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str.include?\"Stinger\""])
    JumpToLabel(["insect"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393] == \"$Hornet\" || $game_variables[393] == \"InsHeir\""])
    Label(["insect"])
    ChangeItems([145, 0, 1, 983])
    ConditionalBranch([0, 2054, 0])
      ConditionalBranch([12, "objective_complete?(33, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 145, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(33,0)"])
          ScriptMore(["call_quest_journal(33)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Comment(["Insects"])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  Script(["@str = $game_variables[393]"])
  ConditionalBranch([12, "@str.include?\"$Bone\""])
    JumpToLabel(["skestr"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str.include?\"$Ske\""])
    JumpToLabel(["skestr"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str[0] == \"$\" && @str[1] == \"B\" && @str[2] == \"o\" && @str[3] == \"n\" && @str[4] == \"e\""])
    Label(["skestr"])
    ChangeItems([146, 0, 1, 983])
    ConditionalBranch([0, 2054, 0])
      ConditionalBranch([12, "objective_complete?(33, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 146, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(33,0)"])
          ScriptMore(["call_quest_journal(33)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Comment(["Skeletons"])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  Script(["@str = $game_variables[393]"])
  ConditionalBranch([12, "@str[1] == \"S\" && @str[2] == \"a\" && @str[3] == \"t\" && @str[4] == \"y\""])
    JumpToLabel(["desstr"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str[0] == \"$\" && @str[1] == \"D\" && @str[2] == \"e\" && @str[3] == \"s\""])
    JumpToLabel(["desstr"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393] == \"$IncubusM\" || $game_variables[393] == \"$IncubusF\""])
    Label(["desstr"])
    ChangeItems([147, 0, 1, 983])
    ConditionalBranch([0, 2054, 0])
      ConditionalBranch([12, "objective_complete?(33, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 147, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(33,0)"])
          ScriptMore(["call_quest_journal(33)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Comment(["Desert"])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  Script(["@str = $game_variables[393]"])
  ConditionalBranch([12, "@str[0] == \"$\" && @str[1] == \"M\" && @str[2] == \"a\" && @str[3] == \"r\" && @str[4] == \"t\""])
    JumpToLabel(["ghostr"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str[0] == \"$\" && @str[1] == \"G\" && @str[2] == \"h\" && @str[3] == \"o\""])
    JumpToLabel(["ghostr"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393] == \"$Ghost\" || $game_variables[393] == \"$GhostCrazed\""])
    Label(["ghostr"])
    Comment(["Ghosts"])
    ChangeItems([148, 0, 1, 983])
    ConditionalBranch([0, 2054, 0])
      ConditionalBranch([12, "objective_complete?(33, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 148, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(33,0)"])
          ScriptMore(["call_quest_journal(33)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  Script(["@str = $game_variables[393]"])
  ConditionalBranch([12, "@str[1] == \"S\" && @str[2] == \"l\" && @str[3] == \"i\" && @str[4] == \"c\""])
    JumpToLabel(["gobstr"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str[0] == \"$\" && @str[1] == \"G\" && @str[2] == \"o\" && @str[3] == \"b\""])
    JumpToLabel(["gobstr"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str[0] == \"G\" && @str[1] == \"o\" && @str[2] == \"b\""])
    JumpToLabel(["gobstr"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393] == \"$Gremlin\""])
    Label(["gobstr"])
    Comment(["Goblins"])
    ChangeItems([149, 0, 1, 983])
    ConditionalBranch([0, 2054, 0])
      ConditionalBranch([12, "objective_complete?(33, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 149, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(33,0)"])
          ScriptMore(["call_quest_journal(33)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  Script(["@str = $game_variables[393]"])
  ConditionalBranch([12, "@str[1] == \"C\" && @str[2] == \"a\" && @str[3] == \"t\" && @str[4] == \"e\""])
    JumpToLabel(["motstr"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str[0] == \"$\" && @str[1] == \"M\" && @str[2] == \"o\" && @str[3] == \"t\" && @str[4] == \"h\""])
    JumpToLabel(["motstr"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str[0] == \"M\" && @str[1] == \"o\" && @str[2] == \"t\" && @str[3] == \"h\""])
    JumpToLabel(["motstr"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393] == \"$Caterpillar\""])
    Label(["motstr"])
    ChangeItems([150, 0, 1, 983])
    ConditionalBranch([0, 2054, 0])
      ConditionalBranch([12, "objective_complete?(33, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 150, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(33,0)"])
          ScriptMore(["call_quest_journal(33)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Comment(["Moths"])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  Script(["@str = $game_variables[393]"])
  ConditionalBranch([12, "@str[0] == \"$\" && @str[1] == \"C\" && @str[2] == \"a\" && @str[3] == \"t\""])
    JumpToLabel(["catstr"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@str[0] == \"C\" && @str[1] == \"a\" && @str[2] == \"t\" && @str[3] == \"S\""])
    JumpToLabel(["catstr"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393] == \"$CatSith\""])
    Label(["catstr"])
    Comment(["Cats"])
    ChangeItems([151, 0, 1, 983])
    ConditionalBranch([0, 2054, 0])
      ConditionalBranch([12, "objective_complete?(33, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 151, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(33,0)"])
          ScriptMore(["call_quest_journal(33)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  Script(["@str = $game_variables[393]"])
  ConditionalBranch([12, "@str[0] == \"$\" && @str[1] == \"R\" && @str[2] == \"a\" && @str[3] == \"b\""])
    JumpToLabel(["rabstr"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_variables[393] == \"$Jackalopes\""])
    Label(["rabstr"])
    Comment(["Rabbits"])
    ChangeItems([152, 0, 1, 983])
    ConditionalBranch([0, 2054, 0])
      ConditionalBranch([12, "objective_complete?(33, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 152, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(33,0)"])
          ScriptMore(["call_quest_journal(33)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  Script(["@str = $game_variables[393]"])
  ConditionalBranch([12, "@str[0] == \"$\" && @str[1] == \"S\" && @str[2] == \"c\" && @str[3] == \"a\" && @str[4] == \"r\" && @str[5] == \"e\""])
    Comment(["Scarecrows"])
    ChangeItems([153, 0, 1, 983])
    ConditionalBranch([0, 2054, 0])
      ConditionalBranch([12, "objective_complete?(33, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 153, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(33,0)"])
          ScriptMore(["call_quest_journal(33)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  Script(["@str = $game_variables[393]"])
  ConditionalBranch([12, "@str[0] == \"$\" && @str[1] == \"F\" && @str[2] == \"i\" && @str[3] == \"n\""])
    Comment(["Finheads / Mermaids"])
    ChangeItems([154, 0, 1, 983])
    ConditionalBranch([0, 2054, 0])
      ConditionalBranch([12, "objective_complete?(33, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 154, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(33,0)"])
          ScriptMore(["call_quest_journal(33)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  Script(["@str = $game_variables[393]"])
  ConditionalBranch([12, "@str.include?\"Bir\""])
    Comment(["Birds"])
    ChangeItems([155, 0, 1, 983])
    ConditionalBranch([0, 2054, 0])
      ConditionalBranch([12, "objective_complete?(33, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 155, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(33,0)"])
          ScriptMore(["call_quest_journal(33)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  Script(["@str = $game_variables[393]"])
  ConditionalBranch([12, "@str.include?\"ForgeSprite\""])
    Comment(["Oni"])
    ChangeItems([156, 0, 1, 983])
    ConditionalBranch([0, 2054, 0])
      ConditionalBranch([12, "objective_complete?(33, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 156, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(33,0)"])
          ScriptMore(["call_quest_journal(33)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  Script(["@str = $game_variables[393]"])
  ConditionalBranch([12, "@str.include?\"WoodGolem\""])
    Comment(["Oni"])
    ChangeItems([157, 0, 1, 983])
    ConditionalBranch([0, 2054, 0])
      ConditionalBranch([12, "objective_complete?(33, 0)"])
        Empty([])

      Else([])
        ConditionalBranch([1, 977, 0, 157, 0])
          ControlSwitches([3000, 3000, 0])
          Wait([15])
          Script(["complete_objective(33,0)"])
          ScriptMore(["call_quest_journal(33)"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    JumpToLabel(["gotit"])
    Empty([])

  BranchEnd([])
  ChangeItems([129, 0, 1, 983])
  Label(["gotit"])
  ControlSwitches([3000, 3000, 1])
  ConditionalBranch([12, "$game_switches[5503] == true"])
    Comment(["### Sex NRG ###"])
    Script(["$game_switches[5503] = false"])
    Script(["$game_switches[5501] = true"])
    Script(["$game_variables[35] = "])
    ScriptMore(["$game_variables[6405]"])
    Script(["$game_variables[6201] = 8888"])
    CallCommonEvent([700])
    Comment(["### Sex NRG ###"])
    Empty([])

  BranchEnd([])
  Empty([])
