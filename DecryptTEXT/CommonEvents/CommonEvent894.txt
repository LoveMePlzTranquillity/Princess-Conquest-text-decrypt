CommonEvent 894
Name = "★👤★ Randomize Ps"

  ShowTextAttributes(["", 0, 0, 2])
  ShowText(["You can decide if you want to play with \\c[18]\"Classic\"\\c[0] or \\c[18]\"Randomized\"\\c[0] Princesses."])
  ShowTextAttributes(["", 0, 0, 2])
  ShowText(["\\c[18]Classic\\c[0]: unique portraits,\\. the canon look of the Princesses,\\. it's the suggested way to play P&C!"])
  ShowTextAttributes(["", 0, 0, 2])
  ShowText(["\\c[18]Randomized\\c[0]: random looks can spice things up,\\. however it's not the recommended setting for new players!\\!"])
  ShowChoices([["Classic Only", "Classic and Randomized", "Randomized Only"], 0])
  When([0, "Classic Only"])
    ControlSwitches([3422, 3422, 0])
    CallCommonEvent([900])
    CallCommonEvent([901])
    CallCommonEvent([902])
    CallCommonEvent([903])
    CallCommonEvent([904])
    CallCommonEvent([905])
    CallCommonEvent([906])
    CallCommonEvent([907])
    CallCommonEvent([908])
    CallCommonEvent([909])
    CallCommonEvent([910])
    CallCommonEvent([911])
    CallCommonEvent([912])
    CallCommonEvent([913])
    CallCommonEvent([914])
    CallCommonEvent([916])
    CallCommonEvent([917])
    CallCommonEvent([918])
    CallCommonEvent([922])
    CallCommonEvent([923])
    CallCommonEvent([921])
    CallCommonEvent([920])
    CallCommonEvent([925])
    ControlSwitches([3422, 3422, 1])
    Empty([])

  When([1, "Classic and Randomized"])
    Script(["@nr = rand(14) + 3"])
    Loop([])
      Script(["@pr = rand(24) + 1"])
      ConditionalBranch([12, "@pr < 2"])
        CallCommonEvent([900])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 3"])
        CallCommonEvent([901])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 4"])
        CallCommonEvent([902])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 5"])
        CallCommonEvent([903])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 6"])
        CallCommonEvent([904])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 7"])
        CallCommonEvent([905])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 8"])
        CallCommonEvent([906])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 9"])
        CallCommonEvent([907])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 10"])
        CallCommonEvent([908])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 11"])
        CallCommonEvent([909])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 12"])
        CallCommonEvent([910])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 13"])
        CallCommonEvent([911])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 14"])
        CallCommonEvent([912])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 15"])
        CallCommonEvent([913])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 16"])
        CallCommonEvent([914])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 17"])
        CallCommonEvent([925])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 18"])
        CallCommonEvent([920])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 19"])
        CallCommonEvent([921])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 20"])
        CallCommonEvent([916])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 21"])
        CallCommonEvent([917])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 22"])
        CallCommonEvent([918])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 23"])
        CallCommonEvent([922])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@pr < 24"])
        CallCommonEvent([923])
        JumpToLabel(["rmz"])
        Empty([])

      BranchEnd([])
      Label(["rmz"])
      Script(["@nr -= 1"])
      ConditionalBranch([12, "@nr <= 0"])
        BreakLoop([])
        Empty([])

      BranchEnd([])
      Empty([])

    RepeatAbove([])
    Empty([])

  When([2, "Randomized Only"])
    Comment(["PQ"])
    CallCommonEvent([900])
    CallCommonEvent([901])
    CallCommonEvent([902])
    CallCommonEvent([903])
    CallCommonEvent([904])
    CallCommonEvent([905])
    CallCommonEvent([906])
    CallCommonEvent([907])
    CallCommonEvent([908])
    CallCommonEvent([909])
    CallCommonEvent([910])
    CallCommonEvent([911])
    CallCommonEvent([912])
    CallCommonEvent([913])
    CallCommonEvent([914])
    CallCommonEvent([916])
    CallCommonEvent([917])
    CallCommonEvent([918])
    CallCommonEvent([922])
    CallCommonEvent([923])
    CallCommonEvent([921])
    CallCommonEvent([925])
    CallCommonEvent([920])
    Comment(["PQ End"])
    Empty([])

  ChoicesEnd([])
  CallCommonEvent([898])
  Empty([])
