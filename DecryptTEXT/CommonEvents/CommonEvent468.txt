CommonEvent 468
Name = "💀 Popper"

  Script(["@atktrig = 0"])
  Script(["@stop = 0"])
  Script(["@trigg = 0"])
  Label(["checkagain"])
  Script(["@prex = $game_player.x"])
  ScriptMore(["@prey = $game_player.y"])
  Script(["@id = @event_id"])
  ScriptMore(["@evix = $game_map.events[@id].x"])
  ScriptMore(["@eviy = $game_map.events[@id].y"])
  Script(["@dx = @prex - @evix"])
  ScriptMore(["@dy = @prey - @eviy"])
  ConditionalBranch([12, "@trigg == 1"])
    JumpToLabel(["nocheck"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_switches[893] == false && $game_switches[2706] == false && $game_switches[1151] == false"])
    ConditionalBranch([12, "@dx.abs + @dy.abs < 6"])
      Script(["@trigg = 1"])
      Label(["nocheck"])
      ConditionalBranch([0, 489, 0])
        Comment(["IGNORED"])
        ShotBalloonIcon([0, 8, true])
        Wait([30])
        JumpToLabel(["checkagain"])
        Empty([])

      Else([])
        ConditionalBranch([0, 986, 0])
          ShotBalloonIcon([0, 6, false])
          SetMoveRoute([0, "RPG::MoveRoute(@repeat=false, @skippable=true, @wait=false, @list=[\"RPG::MoveCommand(@code=11, @parameters=[])\", \"RPG::MoveCommand(@code=0, @parameters=[])\"])"])
          Unnamed(["RPG::MoveCommand(@code=11, @parameters=[])"])
          Wait([30])
          JumpToLabel(["checkagain"])
          Empty([])

        Else([])
          SetMoveRoute([0, "RPG::MoveRoute(@repeat=false, @skippable=true, @wait=false, @list=[\"RPG::MoveCommand(@code=10, @parameters=[])\", \"RPG::MoveCommand(@code=0, @parameters=[])\"])"])
          Unnamed(["RPG::MoveCommand(@code=10, @parameters=[])"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    Else([])
      Wait([5])
      Label(["checkagain"])
      Empty([])

    BranchEnd([])
    Empty([])

  Else([])
    Wait([5])
    Label(["checkagain"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "@atktrig != 0"])
    Script(["@w = 25"])
    Loop([])
      Wait([5])
      Script(["@w -= 5"])
      ConditionalBranch([12, "@w <= 0"])
        BreakLoop([])
        Empty([])

      BranchEnd([])
      Empty([])

    RepeatAbove([])
    Empty([])

  BranchEnd([])
  Script(["@prex = $game_player.x"])
  ScriptMore(["@prey = $game_player.y"])
  Script(["@id = @event_id"])
  ScriptMore(["@evix = $game_map.events[@id].x"])
  ScriptMore(["@eviy = $game_map.events[@id].y"])
  Script(["@dx = @prex - @evix"])
  ScriptMore(["@dy = @prey - @eviy"])
  ConditionalBranch([12, "@dx.abs < 1.5 && @dy.abs < 1.5"])
    Script(["if @atktrig != 1"])
    ScriptMore(["@atktrig = 1"])
    ScriptMore(["else"])
    ScriptMore(["@atkR = 1"])
    ScriptMore(["@atktrig = 0"])
    ScriptMore(["end"])
    ConditionalBranch([12, "@atkR == 1"])
      Script(["@stop = 1"])
      SetMoveRoute([0, "RPG::MoveRoute(@repeat=false, @skippable=false, @wait=false, @list=[\"RPG::MoveCommand(@code=39, @parameters=[])\", \"RPG::MoveCommand(@code=37, @parameters=[])\", \"RPG::MoveCommand(@code=0, @parameters=[])\"])"])
      Unnamed(["RPG::MoveCommand(@code=39, @parameters=[])"])
      Unnamed(["RPG::MoveCommand(@code=37, @parameters=[])"])
      ShowAnimation([0, 182, false])
      Wait([22])
      Script(["@prex = $game_player.x"])
      ScriptMore(["@prey = $game_player.y"])
      Script(["@id = @event_id"])
      ScriptMore(["@evix = $game_map.events[@id].x"])
      ScriptMore(["@eviy = $game_map.events[@id].y"])
      Script(["@dx = @prex - @evix"])
      ScriptMore(["@dy = @prey - @eviy"])
      ConditionalBranch([12, "@dx.abs < 2 && @dy.abs < 2"])
        Script(["$game_map.events[@id].attack"])
        Empty([])

      BranchEnd([])
      ControlSelfSwitch(["B", 0])
      JumpToLabel(["end"])
      Empty([])

    BranchEnd([])
    JumpToLabel(["checkagain"])
    Empty([])

  Else([])
    Script(["@stop = 0"])
    Script(["@atktrig = 0"])
    Empty([])

  BranchEnd([])
  Label(["end"])
  Empty([])
