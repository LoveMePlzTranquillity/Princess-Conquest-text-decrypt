CommonEvent 801
Name = "💀💀💀 Fire 💀💀💀 "

  ConditionalBranch([12, "$game_message.visible == true"])
    JumpToLabel(["hotRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 1770, 0])
    Comment(["All Res"])
    JumpToLabel(["hotRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 4156, 0])
    Comment(["All Res"])
    JumpToLabel(["hotRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 4182, 0])
    Comment(["FLOW"])
    JumpToLabel(["hotRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 3998, 0])
    Comment(["Susp Terr"])
    JumpToLabel(["hotRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 3778, 0])
    Comment(["Ske ending"])
    JumpToLabel(["hotRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.state?(247) == true "])
    Comment(["ClockRES"])
    JumpToLabel(["hotRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.state?(71) == true "])
    Comment(["Cool Aura Skill"])
    JumpToLabel(["hotRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.state?(50) == true "])
    Comment(["COOL drinks"])
    JumpToLabel(["hotRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.equips[5] == $data_armors[457]"])
    Comment(["Slime Flower - Succulent"])
    JumpToLabel(["hotRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.equips[4] == $data_armors[267]"])
    Comment(["CREAM"])
    JumpToLabel(["hotRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.id == 6"])
    Comment(["Golem"])
    ConditionalBranch([12, "$game_party.leader.class.id == 28"])
      Comment(["Succubi"])
      JumpToLabel(["hotRES"])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.id == 4"])
    Comment(["Desert"])
    JumpToLabel(["hotRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.id == 54"])
    Comment(["Oni"])
    JumpToLabel(["hotRES"])
    Empty([])

  BranchEnd([])
  Comment(["UPDATE ONI DATE"])
  ConditionalBranch([12, "$game_party.leader.id >= 35 && $game_party.leader.id <= 39"])
    ControlVariables([2868, 2868, 0, 4, "$game_party.leader.id"])
    ControlVariables([2863, 2863, 0, 4, "$game_party.leader.id"])
    CallCommonEvent([479])
    Script(["@prog = $game_variables[2868]"])
    Comment(["Prog"])
    ConditionalBranch([12, "$game_variables[@prog][55] == 4"])
      Comment(["ShiftCore - HotMod"])
      JumpToLabel(["hotRES"])
      Empty([])

    BranchEnd([])
    Comment(["NEWRES"])
    Script(["@ski = $game_variables[@prog][60] rescue nil"])
    ConditionalBranch([12, "@ski"])
      Comment(["-------------------------------"])
      ConditionalBranch([12, "@ski == 0"])
        JumpToLabel(["afterSkills"])
        Empty([])

      BranchEnd([])
      Comment(["-------------------------------"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "@ski != 0"])
      ConditionalBranch([12, "@ski == \"ThiccFat\""])
        JumpToLabel(["hotRES"])
        Empty([])

      BranchEnd([])
      ConditionalBranch([12, "@ski == \"Immortal\""])
        JumpToLabel(["hotRES"])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Label(["afterSkills"])
    Script(["@resStr = $game_variables[@prog][59]"])
    ConditionalBranch([12, "@resStr != 0"])
      ConditionalBranch([12, "@resStr.include? \"Hot\""])
        JumpToLabel(["hotRES"])
        Empty([])

      BranchEnd([])
      JumpToLabel(["checkRes"])
      Empty([])

    BranchEnd([])
    Comment(["^^^ NEWRES ^^^"])
    ConditionalBranch([12, "$game_variables[@prog][20] == \"seji\""])
      JumpToLabel(["hotRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"nightmare\""])
      JumpToLabel(["hotRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"zmeu\""])
      JumpToLabel(["hotRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"chain\""])
      JumpToLabel(["hotRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"scorpio\""])
      JumpToLabel(["hotRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"sala\""])
      JumpToLabel(["hotRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"dummy\""])
      JumpToLabel(["hotRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"kitsune\""])
      JumpToLabel(["hotRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"anubis\""])
      JumpToLabel(["hotRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"crystal\""])
      JumpToLabel(["hotRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == 7"])
      Comment(["desert"])
      JumpToLabel(["hotRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == 20"])
      Comment(["oni"])
      JumpToLabel(["hotRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == 22"])
      Comment(["lamia"])
      JumpToLabel(["hotRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == 24"])
      Comment(["crystal"])
      JumpToLabel(["hotRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == 16 && $game_variables[@prog][4] == 1"])
      Comment(["Red Dra"])
      JumpToLabel(["hotRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == 16 && $game_variables[@prog][4] == 0"])
      Comment(["Yellow Dra"])
      JumpToLabel(["hotRES"])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  Label(["checkRes"])
  ConditionalBranch([0, 488, 0])
    JumpToLabel(["inAfflict"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 3706, 0])
    ConditionalBranch([2, "A", 1])
      ControlSelfSwitch(["A", 0])
      ShowTextAttributes(["", 0, 0, 2])
      ShowText(["This area is \\c[18]Scorching\\c[0]!\\.\\. You will take damage over time if your Party Leader doesn't resist it!"])
      ShowTextAttributes(["", 0, 0, 2])
      ShowText(["Equip the leader with \\i[685] \\c[18]Fire Resisting\\[0] equipments or have someone naturally resisting it as the Leader!"])
      Empty([])

    BranchEnd([])
    Label(["inAfflict"])
    PlaySE(["RPG::SE(@name=\"Fire1\", @pitch=100, @volume=80)"])
    FlashScreen(["Color(@red=187.0, @green=0.0, @blue=0.0, @alpha=136.0)", 10, false])
    ConditionalBranch([12, "$game_map.map_id == 70"])
      ConditionalBranch([0, 3706, 0])
        Script(["@dmg = 45"])
        Empty([])

      Else([])
        Script(["@dmg = 30"])
        Empty([])

      BranchEnd([])
      Empty([])

    Else([])
      ConditionalBranch([0, 3706, 0])
        Script(["@dmg = 100"])
        Empty([])

      Else([])
        Script(["@dmg = 75"])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    Script(["while $game_party.leader.hp - @dmg < 1"])
    ScriptMore([" @dmg -= 1"])
    ScriptMore(["end"])
    ScriptMore(["$game_party.leader.hp -= @dmg"])
    ScriptMore(["message = @dmg.to_s"])
    ScriptMore(["$game_map.show_text($game_player,message)"])
    ScriptMore(["$game_map.sas_hud.refresh_bars"])
    ConditionalBranch([12, "$game_party.leader.equips[4] == $data_armors[492]"])
      Comment(["Shifting Core"])
      Script(["@r = rand(100)"])
      ConditionalBranch([12, "@r >= 98"])
        ConditionalBranch([12, "$game_party.leader.id >= 35 && $game_party.leader.id <= 39"])
          Script(["@ac = $game_party.leader.id"])
          Script(["@name = $game_actors[@ac].name"])
          Script(["@prog = 2250"])
          ScriptMore(["while @prog <= 2750"])
          ScriptMore(["if @name == $game_variables[@prog][2]"])
          ScriptMore(["break"])
          ScriptMore(["else"])
          ScriptMore(["@prog += 1"])
          ScriptMore(["end"])
          ScriptMore(["end"])
          ShowAnimation([-1, 147, true])
          ShowTextAttributes(["", 0, 0, 2])
          ShowText(["Thanks to the \\i[997] Shifting Core,\\. your Progeny has been granted \\i[685]!"])
          Script(["$game_variables[@prog][55] = 4"])
          Comment(["#####################"])
          Script(["$PU_AUTOMATIC_POPUP = false"])
          Script(["$game_actors[@ac].change_equip(4,$data_nil)"])
          ChangeArmor([492, 1, 0, 1, false])
          Script(["$PU_AUTOMATIC_POPUP = true"])
          Comment(["#####################"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_party.leader.hp <= 1"])
      ControlSwitches([666, 666, 0])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  Label(["hotRES"])
  Empty([])
