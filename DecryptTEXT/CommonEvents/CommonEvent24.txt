CommonEvent 24
Name = "★🧸ß★ Dragon roar"

  ConditionalBranch([0, 4600, 0])
    JumpToLabel(["FFFFF"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 3433, 0])
    JumpToLabel(["end"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 579, 0])
    CallCommonEvent([42])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 97, 1])
    Label(["FFFFF"])
    ConditionalBranch([12, "!$data_system.title1_name.include?\"Stable\""])
      Comment(["BETA"])
      Script(["@weS = [36,37,38,62,109,192,387,637,1359,1550,3548,3755,3923,4163,4323,4423,4492,4703]"])
      Script(["@wplus = [98,99,100,101,141,316,432,752,1610,1980,2872,3264,3987,"])
      ScriptMore(["4220,4675,3121,2801,1697]"])
      Empty([])

    Else([])
      Comment(["STABLE"])
      Script(["@weS = [36,37,38,62,109,192,387,637,1359,1550,3548,3755,3923,4163,4323,4423,4492,0]"])
      Script(["@wplus = [98,99,100,101,141,316,432,752,1610,1980,2872,3264,3987,"])
      ScriptMore(["4220,4675,3121,2801,0]"])
      Empty([])

    BranchEnd([])
    Script(["@i = 0"])
    ScriptMore(["while @i < @wplus.length"])
    ScriptMore(["@weSW = @wplus[@i]"])
    ScriptMore(["if @weSW != 0"])
    ScriptMore(["$game_switches[@weSW] = false"])
    ScriptMore(["end"])
    ScriptMore(["@i += 1"])
    ScriptMore(["end"])
    ConditionalBranch([0, 70, 1])
      Script(["@weS[3] = 0"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([0, 410, 1])
      Script(["@weS[6] = 0"])
      Empty([])

    BranchEnd([])
    Script(["@i = 0"])
    ScriptMore(["while @i < @weS.length"])
    ScriptMore(["@weV = @weS[@i]"])
    ScriptMore(["if @weV != 0"])
    ScriptMore(["if $game_variables[@weV] < 4"])
    ScriptMore([" @weS[@i] = 0"])
    ScriptMore(["end"])
    ScriptMore(["end"])
    ScriptMore(["@i += 1"])
    ScriptMore(["end"])
    ScriptMore(["@weRND = 0"])
    ScriptMore(["if @weS.max > 0"])
    ScriptMore(["while @weRND == 0"])
    ScriptMore(["@weRND = @weS.sample"])
    ScriptMore(["end"])
    ScriptMore(["end"])
    ConditionalBranch([12, "@weRND != 0"])
      ControlSwitches([97, 97, 0])
      ControlVariables([378, 378, 1, 4, "$game_variables[@weRND]"])
      Script(["$game_variables[@weRND] -= 2"])
      Script(["@i = 0"])
      ScriptMore(["while @i < @weS.length"])
      ScriptMore(["if @weRND == @weS[@i]"])
      ScriptMore(["@weSW = @wplus[@i]"])
      ScriptMore(["$game_switches[@weSW] = true"])
      ScriptMore(["end"])
      ScriptMore(["@i += 1"])
      ScriptMore(["end"])
      Comment(["#####################"])
      ControlVariables([4133, 4133, 0, 0, 1])
      CallCommonEvent([735])
      Comment(["#####################"])
      Wait([30])
      ConditionalBranch([0, 96, 1])
        ControlSwitches([96, 96, 0])
        ShowTextAttributes(["", 0, 0, 1])
        ShowText(["Lacomia,\\. the Dragon Princess,\\. has appeared somewhere in the Kingdom."])
        ShowTextAttributes(["", 0, 0, 1])
        ShowText(["This means the Princesses are massing a huge amount of \\i[671] \\c[14]Wealth\\c[0]!"])
        ShowTextAttributes(["", 0, 0, 1])
        ShowText(["You see,\\. this Dragon Princess is really avid,\\. she will go after the Reign with the highest amount of \\i[671] \\c[14]Wealth\\c[0] right now."])
        ShowTextAttributes(["", 0, 0, 1])
        ShowText(["They can't stop the dragon from invading a Reign in any way but,\\. while she will take some of their gold,\\. she will also protect them from invaders!"])
        Empty([])

      Else([])
        ShowTextAttributes(["", 0, 0, 1])
        ShowText(["\\^Lacomia,\\. the Dragon Princess,\\. has appeared somewhere in the Kingdom.\\|"])
        Empty([])

      BranchEnd([])
      Comment(["#####################"])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  Label(["found"])
  ConditionalBranch([12, "!$data_system.title1_name.include?\"Stable\""])
    Comment(["BETA"])
    Script(["@weS = [36,37,38,62,109,192,387,637,1359,1550,3548,3755,"])
    ScriptMore(["3923,4163,4323,4423,4492,4703]"])
    Empty([])

  Else([])
    Comment(["STABLE"])
    Script(["@weS = [36,37,38,62,109,192,387,637,1359,1550,3548,3755,"])
    ScriptMore(["3923,4163,4323,4423,4492,0]"])
    Empty([])

  BranchEnd([])
  Script(["@i = 0"])
  ScriptMore(["while @i < @weS.length"])
  ScriptMore(["@weV = @weS[@i]"])
  ScriptMore(["if @weV != 0"])
  ScriptMore(["if $game_variables[@weV] < 0"])
  ScriptMore([" $game_variables[@weV] = 0"])
  ScriptMore(["end"])
  ScriptMore(["end"])
  ScriptMore(["@i += 1"])
  ScriptMore(["end"])
  Label(["end"])
  ControlSwitches([2569, 2569, 1])
  Empty([])
