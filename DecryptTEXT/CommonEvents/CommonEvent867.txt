CommonEvent 867
Name = "★ 💀💀💀 Poison 💀💀💀 "

  ConditionalBranch([0, 3998, 0])
    Comment(["Ske/Cat ending"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_message.visible == true"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 1770, 0])
    Comment(["All Res"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 4182, 0])
    Comment(["FLOW"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 4156, 0])
    Comment(["All Res"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.state?(246) == true "])
    Comment(["ClockRES"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.state?(247) == true "])
    Comment(["ClockRES"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.state?(59) == true "])
    Comment(["Poison Res state"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.state?(193) == true "])
    Comment(["Poison Res state"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 3998, 0])
    Comment(["Ske ending"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 3778, 0])
    Comment(["Ske ending"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.id == 1 && $game_actors[1].equips[5] == $data_armors[254]"])
    Comment(["Nerv Mask"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.id == 6"])
    Comment(["Golem"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.id == 7"])
    Comment(["Slime"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.id == 9"])
    Comment(["Skele"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.id == 12"])
    Comment(["Ghost"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.equips.include?$data_armors[324]"])
    Comment(["Veleno Cap"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.equips.include?$data_armors[742]"])
    Comment(["Kambucha Shirt"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.equips.include?$data_armors[768]"])
    Comment(["Shadow Q"])
    JumpToLabel(["poiRES"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([12, "$game_party.leader.id >= 35 && $game_party.leader.id <= 39"])
    ControlVariables([2868, 2868, 0, 4, "$game_party.leader.id"])
    ControlVariables([2863, 2863, 0, 4, "$game_party.leader.id"])
    CallCommonEvent([479])
    Script(["@prog = $game_variables[2868]"])
    Comment(["Prog"])
    ConditionalBranch([12, "$game_variables[@prog][55] == 3"])
      Comment(["shiftcore-poison"])
      JumpToLabel(["poiRES"])
      Empty([])

    BranchEnd([])
    Comment(["NEWRES"])
    Script(["@resStr= $game_variables[@prog][59] rescue 0"])
    ConditionalBranch([12, "@resStr != 0"])
      ConditionalBranch([12, "@resStr.include? \"Poi\""])
        JumpToLabel(["poiRES"])
        Empty([])

      BranchEnd([])
      JumpToLabel(["checkRes"])
      Empty([])

    BranchEnd([])
    Comment(["^^^ NEWRES ^^^"])
    ConditionalBranch([12, "$game_variables[@prog][0] == 3 || $game_variables[@prog][0] == 4 || $game_variables[@prog][0] == 6 || $game_variables[@prog][0] == 8 || $game_variables[@prog][0] == 21 || $game_variables[@prog][0] == 22 || $game_variables[@prog][0] == 24 || $game_variables[@prog][0] == 30"])
      Comment(["sli / gol / ske / gho / dri / lam / cry / pla"])
      JumpToLabel(["poiRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"crystal\""])
      JumpToLabel(["poiRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"dummy\""])
      JumpToLabel(["poiRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"squid\""])
      JumpToLabel(["poiRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"dullanyan\""])
      JumpToLabel(["poiRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"sala\""])
      JumpToLabel(["poiRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"scorpio\""])
      JumpToLabel(["poiRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"yukionna\""])
      JumpToLabel(["poiRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"bot\""])
      JumpToLabel(["poiRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"mama\""])
      JumpToLabel(["poiRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"shroom\""])
      JumpToLabel(["poiRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"lilbug\""])
      JumpToLabel(["poiRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == \"lovesnek\""])
      JumpToLabel(["poiRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == 16 && $game_variables[@prog][4] == 3"])
      JumpToLabel(["poiRES"])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[@prog][0] == 16 && $game_variables[@prog][4] == 4"])
      JumpToLabel(["poiRES"])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  Label(["checkRes"])
  ConditionalBranch([0, 487, 0])
    JumpToLabel(["inAfflict"])
    Empty([])

  BranchEnd([])
  ConditionalBranch([0, 3450, 0])
    ConditionalBranch([2, "A", 1])
      ControlSelfSwitch(["A", 0])
      ShowTextAttributes(["", 0, 0, 2])
      ShowText(["This area is \\c[18]Poisonous\\c[0]!\\.\\. You will take damage over time if your Party Leader doesn't resist it!"])
      ShowTextAttributes(["", 0, 0, 2])
      ShowText(["Equip the leader with \\i[682] \\c[18]Poison Resisting\\[0] equipments or have someone naturally resisting it as the Leader!"])
      Empty([])

    BranchEnd([])
    Label(["inAfflict"])
    PlaySE(["RPG::SE(@name=\"Poison\", @pitch=100, @volume=80)"])
    FlashScreen(["Color(@red=170.0, @green=102.0, @blue=221.0, @alpha=136.0)", 10, false])
    ConditionalBranch([12, "$game_map.map_id == 61 || $game_map.map_id == 340 || $game_map.map_id == 453"])
      ConditionalBranch([0, 487, 0])
        Script(["@dmg = 60"])
        Empty([])

      Else([])
        Script(["@dmg = 30"])
        Empty([])

      BranchEnd([])
      Empty([])

    Else([])
      ConditionalBranch([0, 487, 0])
        Script(["@dmg = 30"])
        Empty([])

      Else([])
        Script(["@dmg = 15"])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_variables[116] == \"Toxic\""])
      Comment(["Toxic Heals w poison"])
      Script(["$game_party.leader.hp += @dmg"])
      ScriptMore(["message = \"+\" + @dmg.to_s"])
      ScriptMore(["$game_map.show_text($game_player,message)"])
      ScriptMore(["$game_map.sas_hud.refresh_bars"])
      Empty([])

    Else([])
      Script(["while $game_party.leader.hp - @dmg < 1"])
      ScriptMore([" @dmg -= 1"])
      ScriptMore(["end"])
      ScriptMore(["$game_party.leader.hp -= @dmg"])
      ScriptMore(["message = @dmg.to_s"])
      ScriptMore(["$game_map.show_text($game_player,message)"])
      ScriptMore(["$game_map.sas_hud.refresh_bars"])
      Empty([])

    BranchEnd([])
    Script([""])
    ConditionalBranch([12, "$game_party.leader.equips[4] == $data_armors[492]"])
      Comment(["Shifting Core"])
      Script(["@r = rand(100)"])
      ConditionalBranch([12, "@r >= 98"])
        ConditionalBranch([12, "$game_party.leader.id >= 35 && $game_party.leader.id <= 39"])
          Script(["@ac = $game_party.leader.id"])
          Script(["@name = $game_actors[@ac].name"])
          Comment(["#########"])
          Script(["@prog = 2250"])
          ScriptMore(["while @prog <= 2750"])
          ScriptMore(["if @name == $game_variables[@prog][2]"])
          ScriptMore(["break"])
          ScriptMore(["else"])
          ScriptMore(["@prog += 1"])
          ScriptMore(["end"])
          ScriptMore(["end"])
          Comment(["#########"])
          ShowAnimation([-1, 147, true])
          ShowTextAttributes(["", 0, 0, 2])
          ShowText(["Thanks to the \\i[997] Shifting Core,\\. your Progeny has been granted \\i[682]!"])
          Script(["$game_variables[@prog][55] = 3"])
          Comment(["#####################"])
          Script(["$PU_AUTOMATIC_POPUP = false"])
          Script(["$game_actors[@ac].change_equip(4,$data_nil)"])
          ChangeArmor([492, 1, 0, 1, false])
          Script(["$PU_AUTOMATIC_POPUP = true"])
          Comment(["#####################"])
          Empty([])

        BranchEnd([])
        Empty([])

      BranchEnd([])
      Empty([])

    BranchEnd([])
    ConditionalBranch([12, "$game_party.leader.hp <= 1"])
      ControlSwitches([666, 666, 0])
      Empty([])

    BranchEnd([])
    Empty([])

  BranchEnd([])
  Label(["poiRES"])
  Empty([])
